# Dynamic Modules 통합 구현 가이드

## 📋 개요

IMCAT UI의 Dynamic Modules는 관련 기능을 그룹화하여 **17개의 통합 모듈**로 구성됩니다.
각 모듈은 JavaScript가 필요한 인터랙티브 컴포넌트들을 포함하며, 공통 로직을 공유하여 효율적으로 설계되었습니다.

---

## 🎯 통합 모듈 목록 (17개)

### **1. Overlays & Dialogs**

**파일:** `overlays.js`  
**아이콘:** web_asset

**포함 기능:**

- Modal (모달 다이얼로그)
- Drawer (사이드 패널)
- Offcanvas (오프캔버스)
- Lightbox (이미지 갤러리)

**주요 특징:**

- 공통 오버레이 배경 처리
- ESC 키 닫기
- 포커스 트랩
- 스크롤 방지

**사용 예시:**

```javascript
const Overlays = await IMCAT.use('overlays');

// Modal
const modal = new Overlays.Modal({
  title: '제목',
  content: '내용',
  onClose: () => {}
});

// Drawer
const drawer = new Overlays.Drawer({
  position: 'right',
  content: '...'
});

// Lightbox
const lightbox = new Overlays.Lightbox({
  images: ['url1', 'url2']
});
```

---

### **2. Navigation Components**

**파일:** `navigation.js`  
**아이콘:** tab

**포함 기능:**

- Tabs (탭 네비게이션)
- Accordion (아코디언)
- Collapse (접기/펼치기)
- Mega Menu (메가 메뉴)
- Tree View (트리 구조)

**주요 특징:**

- ARIA 속성 자동 관리
- 중첩 구조 지원
- 애니메이션 전환

**사용 예시:**

```javascript
const Navigation = await IMCAT.use('navigation');

// Tabs
const tabs = new Navigation.Tabs({
  container: '#tabs-container'
});

// Accordion
const accordion = new Navigation.Accordion({
  container: '#accordion',
  multiple: false
});

// Tree View
const tree = new Navigation.TreeView({
  data: [...],
  onNodeClick: (node) => {}
});

// Context Menu
const contextMenu = new Navigation.ContextMenu({
  items: [
    { label: '복사', icon: 'content_copy', action: () => {} },
    { label: '붙여넣기', icon: 'content_paste', action: () => {} }
  ]
});
```

---

### **3. Pickers (Date/Time/Color)**

**파일:** `pickers.js`  
**아이콘:** calendar_today

**포함 기능:**

- Date Picker (날짜 선택)
- Time Picker (시간 선택)
- Color Picker (색상 선택)

**주요 특징:**

- 통일된 드롭다운 UI
- 포맷 커스터마이징
- i18n 지원
- 모바일 최적화

**사용 예시:**

```javascript
const Pickers = await IMCAT.use('pickers');

// Date Picker
const datePicker = new Pickers.Date({
  input: '#date',
  format: 'YYYY-MM-DD',
  minDate: '2020-01-01',
  onChange: (date) => {}
});

// Time Picker
const timePicker = new Pickers.Time({
  input: '#time',
  format: 'HH:mm',
  step: 15
});

// Color Picker
const colorPicker = new Pickers.Color({
  input: '#color',
  format: 'hex',
  onChange: (color) => {}
});
```

---

### **4. Advanced Selectors**

**파일:** `selectors.js`  
**아이콘:** search

**포함 기능:**

- Autocomplete (자동완성)
- Multi Select (다중 선택)
- Range Selector (범위 선택)

**주요 특징:**

- 검색 필터링
- 키보드 네비게이션
- 태그 생성/제거
- 비동기 데이터 지원

**사용 예시:**

```javascript
const Selectors = await IMCAT.use('selectors');

// Autocomplete
const autocomplete = new Selectors.Autocomplete({
  input: '#search',
  source: async (query) => {
    // API 호출
    return results;
  },
  onSelect: (item) => {}
});

// Multi Select
const multiSelect = new Selectors.MultiSelect({
  input: '#tags',
  options: [...],
  maxItems: 10
});

// Range Selector
const rangeSelector = new Selectors.Range({
  min: 0,
  max: 1000,
  step: 10,
  onChange: (values) => {}
});
```

---

### **5. Form Components**

**파일:** `form-components.js`  
**아이콘:** upload_file

**포함 기능:**

- File Upload (파일 업로드)
- Form Validation (폼 검증)
- Form Wizard (단계별 폼)
- Signature Pad (서명 패드)
- Rating Input (별점 입력)

**주요 특징:**

- 드래그 앤 드롭
- 실시간 검증
- 단계 진행 관리
- 터치/마우스 지원

**사용 예시:**

```javascript
const FormComponents = await IMCAT.use('form-components');

// File Upload
const uploader = new FormComponents.FileUpload({
  input: '#file',
  accept: 'image/*',
  multiple: true,
  onUpload: (files) => {}
});

// Form Validation
const validator = new FormComponents.Validation({
  form: '#myForm',
  rules: {
    email: 'required|email',
    password: 'required|min:8'
  }
});

// Form Wizard
const wizard = new FormComponents.Wizard({
  container: '#wizard',
  steps: [...],
  onComplete: (data) => {}
});

// Signature Pad
const signature = new FormComponents.Signature({
  canvas: '#signature',
  onSave: (dataUrl) => {}
});

// Rating Input
const rating = new FormComponents.Rating({
  container: '#rating',
  max: 5,
  onChange: (value) => {}
});
```

---

### **6. Feedback & Loading**

**파일:** `feedback.js`  
**아이콘:** notifications

**포함 기능:**

- Notifications (알림)
- Toast (토스트 메시지)
- Progress Tracker (진행 추적)
- Skeleton Loader (로딩 플레이스홀더)
- Tour Guide (가이드 투어)

**주요 특징:**

- 자동 사라짐
- 큐 관리
- 위치 지정
- 애니메이션

**사용 예시:**

```javascript
const Feedback = await IMCAT.use('feedback');

// Notification
Feedback.notify({
  title: '성공',
  message: '저장되었습니다',
  type: 'success',
  duration: 3000
});

// Toast
Feedback.toast('작업 완료', 'success');

// Progress Tracker
const tracker = new Feedback.ProgressTracker({
  steps: ['Step 1', 'Step 2', 'Step 3'],
  current: 1
});

// Skeleton Loader
const skeleton = new Feedback.Skeleton({
  container: '#content',
  type: 'card'
});

// Tour Guide
const tour = new Feedback.Tour({
  steps: [
    { element: '#btn', content: '이 버튼을 클릭하세요' }
  ]
});
```

---

### **7. Data Visualization**

**파일:** `data-viz.js`  
**아이콘:** insert_chart

**포함 기능:**

- Data Table (데이터 테이블)
- Charts (차트)
- Masonry Grid (타일 레이아웃)
- Kanban Board (칸반 보드)
- Calendar (캘린더)

**주요 특징:**

- 정렬/필터/검색
- 드래그 앤 드롭
- 반응형 레이아웃
- 데이터 바인딩

**사용 예시:**

```javascript
const DataViz = await IMCAT.use('data-viz');

// Data Table
const table = new DataViz.Table({
  container: '#table',
  data: [...],
  columns: [...],
  sortable: true,
  filterable: true
});

// Charts
const chart = new DataViz.Chart({
  container: '#chart',
  type: 'line',
  data: {...}
});

// Masonry Grid
const masonry = new DataViz.Masonry({
  container: '#grid',
  columnWidth: 300
});

// Kanban Board
const kanban = new DataViz.Kanban({
  container: '#kanban',
  columns: [...],
  onMove: (card, column) => {}
});

// Calendar
const calendar = new DataViz.Calendar({
  container: '#calendar',
  events: [...],
  onDateClick: (date) => {}
});
```

---

### **8. Scroll Components**

**파일:** `scroll-components.js`  
**아이콘:** sync_alt

**포함 기능:**

- Virtual Scroll (가상 스크롤)
- Scrollspy (스크롤 스파이)

**주요 특징:**

- 대용량 데이터 처리
- 무한 스크롤
- 네비게이션 동기화
- 성능 최적화

**사용 예시:**

```javascript
const ScrollComponents = await IMCAT.use('scroll-components');

// Virtual Scroll
const virtualScroll = new ScrollComponents.Virtual({
  container: '#list',
  items: 10000,
  itemHeight: 50,
  renderItem: (index) => `<div>Item ${index}</div>`
});

// Scrollspy
const scrollspy = new ScrollComponents.Scrollspy({
  sections: ['#section1', '#section2'],
  navigation: '#nav',
  offset: 100
});
```

---

### **9. Text Editors**

**파일:** `editors.js`  
**아이콘:** edit

**포함 기능:**

- Rich Text Editor (리치 텍스트)
- Markdown Editor (마크다운)

**주요 특징:**

- WYSIWYG
- 툴바 커스터마이징
- 이미지 업로드
- 프리뷰 모드

**사용 예시:**

```javascript
const Editors = await IMCAT.use('editors');

// Rich Text Editor
const richEditor = new Editors.RichText({
  container: '#editor',
  toolbar: ['bold', 'italic', 'link', 'image'],
  onChange: (html) => {}
});

// Markdown Editor
const mdEditor = new Editors.Markdown({
  container: '#md-editor',
  preview: true,
  onChange: (markdown) => {}
});
```

---

### **10. Media Viewers**

**파일:** `media-viewers.js`  
**아이콘:** play_circle

**포함 기능:**

- Video Player (비디오 플레이어)
- PDF Viewer (PDF 뷰어)

**주요 특징:**

- 커스텀 컨트롤
- 키보드 단축키
- 페이지 네비게이션
- 풀스크린 지원

**사용 예시:**

```javascript
const MediaViewers = await IMCAT.use('media-viewers');

// Video Player
const player = new MediaViewers.Video({
  container: '#player',
  src: 'video.mp4',
  autoplay: false,
  controls: true
});

// PDF Viewer
const pdfViewer = new MediaViewers.PDF({
  container: '#pdf',
  url: 'document.pdf',
  zoom: 1.0
});
```

---

### **11. Social Components**

**파일:** `social-components.js`  
**아이콘:** chat

**포함 기능:**

- Chat Widget (채팅 위젯)
- Comment System (댓글 시스템)
- Share Dialog (공유 다이얼로그)

**주요 특징:**

- 실시간 메시징
- 대댓글 지원
- SNS 공유
- 이모지 지원

**사용 예시:**

```javascript
const SocialComponents = await IMCAT.use('social-components');

// Chat Widget
const chat = new SocialComponents.Chat({
  container: '#chat',
  onMessage: (message) => {}
});

// Comment System
const comments = new SocialComponents.Comments({
  container: '#comments',
  nested: true,
  onComment: (comment) => {}
});

// Share Dialog
const share = new SocialComponents.Share({
  url: window.location.href,
  title: '제목',
  networks: ['facebook', 'twitter', 'kakao']
});
```

---

### **12. Advanced UI**

**파일:** `advanced-ui.js`  
**아이콘:** dashboard_customize

**포함 기능:**

- Split Pane (분할 패널)
- QR Code Generator (QR 코드 생성)

**주요 특징:**

- 크기 조절 가능
- 최소/최대 크기
- QR 커스터마이징
- 다운로드 지원

**사용 예시:**

```javascript
const AdvancedUI = await IMCAT.use('advanced-ui');

// Split Pane
const split = new AdvancedUI.SplitPane({
  container: '#split',
  direction: 'horizontal',
  minSize: 200
});

// QR Code
const qr = new AdvancedUI.QRCode({
  container: '#qr',
  text: 'https://example.com',
  size: 200
});
```

---

## 🔧 단독 모듈 (5개)

### **13. Dropdown**

간단한 드롭다운 메뉴

### **14. Tooltips & Popovers**

툴팁과 팝오버 (이미 통합됨)

### **15. Carousel**

이미지/콘텐츠 슬라이더

### **16. Live Status**

실시간 상태 표시

### **17. Stepper**

단계별 진행 표시

---

## 📦 모듈 구조

```text
src/modules/
├── overlays/
│   ├── overlays.js
│   ├── overlays.scss
│   └── README.md
├── navigation/
│   ├── navigation.js
│   ├── navigation.scss
│   └── README.md
├── pickers/
│   ├── pickers.js
│   ├── pickers.scss
│   └── README.md
└── ...
```

---

## 🎯 구현 패턴

### JavaScript 구조

```javascript
class ModuleName {
  // 하위 클래스 정의
  static SubComponent = class SubComponent {
    static defaults() {
      return { option: 'value' };
    }
    
    constructor(options = {}) {
      this.options = { ...SubComponent.defaults(), ...options };
      this.init();
    }
    
    init() {
      // 초기화
    }
    
    destroy() {
      // 정리
    }
  }
  
  // 편의 메서드
  static create(type, options) {
    return new this[type](options);
  }
}

export default ModuleName;
```

### SCSS 구조

```scss
// overlays.scss
.overlay {
  // 공통 스타일
}

.modal {
  @extend .overlay;
  // Modal 전용
}

.drawer {
  @extend .overlay;
  // Drawer 전용
}
```

---

## ✅ 구현 체크리스트

**각 통합 모듈 구현 시:**

- [ ] JavaScript 클래스 구조
- [ ] 하위 컴포넌트 정의
- [ ] 공통 로직 추출
- [ ] SCSS 스타일 (BEM)
- [ ] HTML 예제 페이지
- [ ] README.md 작성
- [ ] destroy() 메서드
- [ ] ARIA 속성
- [ ] 키보드 네비게이션
- [ ] 반응형 처리
- [ ] 테마 지원 (라이트/다크)
- [ ] JSDoc 주석

---

## 🚀 우선순위

### Phase 1: 필수 (우선 구현)

1. ✅ Overlays & Dialogs - **완료**
2. ✅ Navigation Components - **완료**
3. ✅ Dropdown - **완료**
4. ✅ Pickers - **완료**
5. ✅ Advanced Selectors - **완료**

### Phase 2: 고수요

1. ✅ Form Components - **완료**
2. 🔄 Feedback & Loading - **진행중**
3. Data Visualization

### Phase 3: 고급 기능

1. Scroll Components
2. Text Editors
3. Media Viewers
4. Social Components
5. Advanced UI

---

## 📚 참고 문서

- **코어 API**: `src/core/*.js`
- **모듈 시스템**: `docs/모듈_시스템_설계서.md`
- **디자인 시스템**: `docs/디자인_시스템_구현_가이드.md`
- **빌드 가이드**: `docs/빌드_가이드.md`

---

**17개의 효율적인 통합 모듈로 IMCAT UI를 완성합니다!** 🎉

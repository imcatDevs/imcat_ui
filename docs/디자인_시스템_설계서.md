# IMCAT UI 디자인 시스템 설계서

## 📋 개요

IMCAT UI의 일관된 디자인 시스템 및 SCSS 기반 스타일 아키텍처 설계 문서입니다.

**핵심 원칙:**

- 일관된 디자인 토큰
- 재사용 가능한 컴포넌트
- 테마 시스템 지원
- 중복 방지 (DRY)
- 접근성 우선

---

## 🎨 SCSS 아키텍처

### 디렉토리 구조

```text
imcat-ui/styles/
├── abstracts/              # 변수, 믹스인, 함수
│   ├── _variables.scss    # 디자인 토큰
│   ├── _mixins.scss       # 재사용 믹스인
│   ├── _functions.scss    # 유틸리티 함수
│   └── _themes.scss       # 테마 정의
│
├── base/                   # 기본 스타일
│   ├── _reset.scss        # CSS 리셋
│   ├── _typography.scss   # 타이포그래피
│   └── _animations.scss   # 애니메이션
│
├── components/             # 컴포넌트 스타일
│   ├── _modal.scss
│   ├── _dropdown.scss
│   ├── _tooltip.scss
│   ├── _button.scss
│   └── ...
│
├── core/                   # 코어 스타일
│   ├── _loading.scss
│   └── _router.scss
│
└── imcat-ui.scss          # 메인 진입점
```

---

## 🎯 디자인 토큰 (Design Tokens)

### _variables.scss

```scss
// ============================================
// 색상 시스템 (Color System)
// ============================================

// 기본 색상 팔레트
$primary-color: #007bff !default;
$secondary-color: #6c757d !default;
$success-color: #28a745 !default;
$danger-color: #dc3545 !default;
$warning-color: #ffc107 !default;
$info-color: #17a2b8 !default;
$light-color: #f8f9fa !default;
$dark-color: #343a40 !default;

// 그레이 스케일
$gray-100: #f8f9fa !default;
$gray-200: #e9ecef !default;
$gray-300: #dee2e6 !default;
$gray-400: #ced4da !default;
$gray-500: #adb5bd !default;
$gray-600: #6c757d !default;
$gray-700: #495057 !default;
$gray-800: #343a40 !default;
$gray-900: #212529 !default;

// 텍스트 색상
$text-primary: $gray-900 !default;
$text-secondary: $gray-600 !default;
$text-muted: $gray-500 !default;
$text-disabled: $gray-400 !default;

// 배경 색상
$bg-primary: #ffffff !default;
$bg-secondary: $gray-100 !default;
$bg-dark: $gray-800 !default;

// 테두리 색상
$border-color: $gray-300 !default;
$border-light: $gray-200 !default;
$border-dark: $gray-400 !default;

// ============================================
// 타이포그래피 (Typography)
// ============================================

// 폰트 패밀리
$font-family-base: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 
                   'Helvetica Neue', Arial, sans-serif !default;
$font-family-monospace: 'SFMono-Regular', Consolas, 'Liberation Mono', 
                        Menlo, Monaco, monospace !default;

// 폰트 크기
$font-size-base: 1rem !default;      // 16px
$font-size-xs: 0.75rem !default;     // 12px
$font-size-sm: 0.875rem !default;    // 14px
$font-size-lg: 1.125rem !default;    // 18px
$font-size-xl: 1.25rem !default;     // 20px

$h1-font-size: 2.5rem !default;      // 40px
$h2-font-size: 2rem !default;        // 32px
$h3-font-size: 1.75rem !default;     // 28px
$h4-font-size: 1.5rem !default;      // 24px
$h5-font-size: 1.25rem !default;     // 20px
$h6-font-size: 1rem !default;        // 16px

// 폰트 두께
$font-weight-light: 300 !default;
$font-weight-normal: 400 !default;
$font-weight-medium: 500 !default;
$font-weight-semibold: 600 !default;
$font-weight-bold: 700 !default;

// 라인 높이
$line-height-base: 1.5 !default;
$line-height-sm: 1.25 !default;
$line-height-lg: 2 !default;

// ============================================
// 간격 시스템 (Spacing System)
// ============================================

$spacer: 1rem !default;  // 16px

$spacing: (
  0: 0,
  1: $spacer * 0.25,   // 4px
  2: $spacer * 0.5,    // 8px
  3: $spacer,          // 16px
  4: $spacer * 1.5,    // 24px
  5: $spacer * 2,      // 32px
  6: $spacer * 3,      // 48px
  7: $spacer * 4,      // 64px
  8: $spacer * 5,      // 80px
) !default;

// ============================================
// 크기 (Sizing)
// ============================================

$border-radius-sm: 0.2rem !default;   // 3.2px
$border-radius: 0.25rem !default;     // 4px
$border-radius-lg: 0.3rem !default;   // 4.8px
$border-radius-xl: 0.5rem !default;   // 8px
$border-radius-pill: 50rem !default;  // 완전한 원형

$border-width: 1px !default;
$border-width-lg: 2px !default;

// ============================================
// 그림자 (Shadows)
// ============================================

$shadow-sm: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075) !default;
$shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15) !default;
$shadow-lg: 0 1rem 3rem rgba(0, 0, 0, 0.175) !default;
$shadow-xl: 0 1.5rem 4rem rgba(0, 0, 0, 0.2) !default;

// ============================================
// Z-Index 계층
// ============================================

$zindex-dropdown: 1000 !default;
$zindex-sticky: 1020 !default;
$zindex-fixed: 1030 !default;
$zindex-modal-backdrop: 1040 !default;
$zindex-modal: 1050 !default;
$zindex-popover: 1060 !default;
$zindex-tooltip: 1070 !default;
$zindex-toast: 1080 !default;

// ============================================
// 트랜지션 (Transitions)
// ============================================

$transition-base: all 0.3s ease-in-out !default;
$transition-fast: all 0.15s ease-in-out !default;
$transition-slow: all 0.5s ease-in-out !default;

$transition-fade: opacity 0.15s linear !default;
$transition-collapse: height 0.35s ease !default;
```

---

## 🔧 믹스인 (Mixins)

### _mixins.scss

```scss
// ============================================
// 반응형 믹스인
// ============================================

$breakpoints: (
  xs: 0,
  sm: 576px,
  md: 768px,
  lg: 992px,
  xl: 1200px,
  xxl: 1400px
) !default;

@mixin media-breakpoint-up($name) {
  $min: map-get($breakpoints, $name);
  @if $min {
    @media (min-width: $min) {
      @content;
    }
  } @else {
    @content;
  }
}

@mixin media-breakpoint-down($name) {
  $max: map-get($breakpoints, $name) - 0.02px;
  @media (max-width: $max) {
    @content;
  }
}

// ============================================
// 버튼 믹스인
// ============================================

@mixin button-variant($bg-color, $hover-bg-color: darken($bg-color, 7.5%)) {
  background-color: $bg-color;
  border-color: $bg-color;
  color: color-contrast($bg-color);

  &:hover {
    background-color: $hover-bg-color;
    border-color: darken($bg-color, 10%);
  }

  &:focus {
    box-shadow: 0 0 0 0.2rem rgba($bg-color, 0.5);
  }

  &:active {
    background-color: darken($bg-color, 10%);
  }

  &:disabled {
    background-color: $bg-color;
    border-color: $bg-color;
    opacity: 0.65;
    cursor: not-allowed;
  }
}

// ============================================
// 카드 믹스인
// ============================================

@mixin card($padding: map-get($spacing, 3)) {
  background-color: $bg-primary;
  border: $border-width solid $border-color;
  border-radius: $border-radius;
  padding: $padding;
  box-shadow: $shadow-sm;
}

// ============================================
// 중앙 정렬 믹스인
// ============================================

@mixin center-flex {
  display: flex;
  align-items: center;
  justify-content: center;
}

@mixin center-absolute {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}

// ============================================
// 말줄임표 믹스인
// ============================================

@mixin text-ellipsis($lines: 1) {
  @if $lines == 1 {
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  } @else {
    display: -webkit-box;
    -webkit-line-clamp: $lines;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
}

// ============================================
// 스크롤바 커스터마이징
// ============================================

@mixin custom-scrollbar($width: 8px, $track-color: $gray-200, $thumb-color: $gray-400) {
  &::-webkit-scrollbar {
    width: $width;
    height: $width;
  }

  &::-webkit-scrollbar-track {
    background: $track-color;
  }

  &::-webkit-scrollbar-thumb {
    background: $thumb-color;
    border-radius: $border-radius;

    &:hover {
      background: darken($thumb-color, 10%);
    }
  }
}
```

---

## 🎨 테마 시스템

### _themes.scss

```scss
// ============================================
// 다크 테마
// ============================================

$dark-theme: (
  'bg-primary': #1a1a1a,
  'bg-secondary': #2d2d2d,
  'text-primary': #ffffff,
  'text-secondary': #b0b0b0,
  'border-color': #3d3d3d,
  'shadow': 0 0.5rem 1rem rgba(0, 0, 0, 0.5)
) !default;

// ============================================
// 테마 적용 믹스인
// ============================================

@mixin apply-theme($theme-map) {
  @each $key, $value in $theme-map {
    --#{$key}: #{$value};
  }
}

// ============================================
// 테마 사용
// ============================================

.imcat-theme-dark {
  @include apply-theme($dark-theme);
}

// CSS 변수로 테마 적용
:root {
  --primary-color: #{$primary-color};
  --secondary-color: #{$secondary-color};
  --success-color: #{$success-color};
  --danger-color: #{$danger-color};
  --warning-color: #{$warning-color};
  --info-color: #{$info-color};
  
  --bg-primary: #{$bg-primary};
  --bg-secondary: #{$bg-secondary};
  
  --text-primary: #{$text-primary};
  --text-secondary: #{$text-secondary};
  
  --border-color: #{$border-color};
  --border-radius: #{$border-radius};
  
  --shadow: #{$shadow};
  --shadow-sm: #{$shadow-sm};
  --shadow-lg: #{$shadow-lg};
}
```

---

## 📦 컴포넌트 스타일 설계

### 버튼 컴포넌트 (_button.scss)

```scss
// ============================================
// 버튼 기본 스타일
// ============================================

.imcat-btn {
  display: inline-block;
  font-family: $font-family-base;
  font-size: $font-size-base;
  font-weight: $font-weight-normal;
  line-height: $line-height-base;
  text-align: center;
  text-decoration: none;
  white-space: nowrap;
  vertical-align: middle;
  user-select: none;
  border: $border-width solid transparent;
  padding: map-get($spacing, 2) map-get($spacing, 3);
  border-radius: $border-radius;
  transition: $transition-base;
  cursor: pointer;

  &:hover {
    text-decoration: none;
  }

  &:focus {
    outline: 0;
    box-shadow: 0 0 0 0.2rem rgba($primary-color, 0.25);
  }

  &:disabled {
    opacity: 0.65;
    cursor: not-allowed;
  }
}

// ============================================
// 버튼 변형
// ============================================

.imcat-btn-primary {
  @include button-variant($primary-color);
}

.imcat-btn-secondary {
  @include button-variant($secondary-color);
}

.imcat-btn-success {
  @include button-variant($success-color);
}

.imcat-btn-danger {
  @include button-variant($danger-color);
}

// ============================================
// 버튼 크기
// ============================================

.imcat-btn-sm {
  padding: map-get($spacing, 1) map-get($spacing, 2);
  font-size: $font-size-sm;
  border-radius: $border-radius-sm;
}

.imcat-btn-lg {
  padding: map-get($spacing, 3) map-get($spacing, 4);
  font-size: $font-size-lg;
  border-radius: $border-radius-lg;
}

// ============================================
// 버튼 상태
// ============================================

.imcat-btn-block {
  display: block;
  width: 100%;
}

.imcat-btn-outline {
  background-color: transparent;
  border-color: currentColor;

  &:hover {
    background-color: currentColor;
    color: #fff;
  }
}
```

### 모달 컴포넌트 (_modal.scss)

```scss
// ============================================
// 모달 기본 스타일
// ============================================

.imcat-modal {
  position: fixed;
  top: 0;
  left: 0;
  z-index: $zindex-modal;
  width: 100%;
  height: 100%;
  overflow-x: hidden;
  overflow-y: auto;
  outline: 0;
  display: none;

  &.imcat-modal-show {
    display: block;
  }
}

// ============================================
// 모달 백드롭
// ============================================

.imcat-modal-backdrop {
  position: fixed;
  top: 0;
  left: 0;
  z-index: $zindex-modal-backdrop;
  width: 100vw;
  height: 100vh;
  background-color: rgba(#000, 0.5);
  opacity: 0;
  transition: $transition-fade;

  &.imcat-modal-backdrop-show {
    opacity: 1;
  }
}

// ============================================
// 모달 다이얼로그
// ============================================

.imcat-modal-dialog {
  position: relative;
  width: auto;
  max-width: 500px;
  margin: map-get($spacing, 5) auto;
  pointer-events: none;
  transform: translateY(-50px);
  transition: transform 0.3s ease-out;

  .imcat-modal-show & {
    transform: translateY(0);
  }
}

// ============================================
// 모달 콘텐츠
// ============================================

.imcat-modal-content {
  position: relative;
  display: flex;
  flex-direction: column;
  width: 100%;
  pointer-events: auto;
  background-color: var(--bg-primary, $bg-primary);
  border: $border-width solid var(--border-color, $border-color);
  border-radius: $border-radius;
  box-shadow: $shadow-lg;
  outline: 0;
}

// ============================================
// 모달 헤더
// ============================================

.imcat-modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: map-get($spacing, 3);
  border-bottom: $border-width solid var(--border-color, $border-color);
  border-top-left-radius: $border-radius;
  border-top-right-radius: $border-radius;
}

.imcat-modal-title {
  margin: 0;
  font-size: $h5-font-size;
  font-weight: $font-weight-medium;
  line-height: $line-height-base;
  color: var(--text-primary, $text-primary);
}

.imcat-modal-close {
  padding: 0;
  background-color: transparent;
  border: 0;
  font-size: 1.5rem;
  font-weight: $font-weight-bold;
  line-height: 1;
  color: var(--text-primary, $text-primary);
  opacity: 0.5;
  cursor: pointer;
  transition: $transition-base;

  &:hover {
    opacity: 0.75;
  }

  &:focus {
    outline: 0;
    opacity: 1;
  }
}

// ============================================
// 모달 바디
// ============================================

.imcat-modal-body {
  position: relative;
  flex: 1 1 auto;
  padding: map-get($spacing, 3);
  color: var(--text-primary, $text-primary);
}

// ============================================
// 모달 푸터
// ============================================

.imcat-modal-footer {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: flex-end;
  padding: map-get($spacing, 3);
  border-top: $border-width solid var(--border-color, $border-color);
  border-bottom-left-radius: $border-radius;
  border-bottom-right-radius: $border-radius;

  > * {
    margin-left: map-get($spacing, 2);
  }
}

// ============================================
// 반응형
// ============================================

@include media-breakpoint-down(sm) {
  .imcat-modal-dialog {
    max-width: 100%;
    margin: map-get($spacing, 2);
  }
}
```

---

## 📏 코딩 규칙

### 네이밍 컨벤션

```scss
// BEM 방법론 사용
.block {}
.block__element {}
.block--modifier {}

// 예시
.imcat-modal {}
.imcat-modal__header {}
.imcat-modal__title {}
.imcat-modal--large {}
```

### 파일 구조 규칙

```scss
// 1. 변수
$component-padding: map-get($spacing, 3);

// 2. 믹스인
@mixin component-state() { }

// 3. 기본 스타일
.component { }

// 4. 요소 스타일
.component__element { }

// 5. 수정자 스타일
.component--modifier { }

// 6. 상태 스타일
.component.is-active { }

// 7. 반응형
@include media-breakpoint-down(sm) { }
```

### 주석 규칙

```scss
// ============================================
// 섹션 제목
// ============================================

// 단일 라인 주석

/**
 * 다중 라인 주석
 * 복잡한 설명이 필요할 때
 */
```

---

## 🎯 접근성 가이드

```scss
// 포커스 스타일
.imcat-component:focus {
  outline: 2px solid $primary-color;
  outline-offset: 2px;
}

// 스크린 리더 전용
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

// 고대비 모드
@media (prefers-contrast: high) {
  .imcat-component {
    border-width: $border-width-lg;
  }
}

// 모션 축소
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

---

## 📝 메인 진입점

### imcat-ui.scss

```scss
// ============================================
// IMCAT UI 메인 스타일시트
// ============================================

// 1. Abstracts
@import 'abstracts/variables';
@import 'abstracts/functions';
@import 'abstracts/mixins';
@import 'abstracts/themes';

// 2. Base
@import 'base/reset';
@import 'base/typography';
@import 'base/animations';

// 3. Core
@import 'core/loading';
@import 'core/router';

// 4. Components
@import 'components/button';
@import 'components/modal';
@import 'components/dropdown';
@import 'components/tooltip';
@import 'components/tabs';
@import 'components/alert';
@import 'components/toast';
```

---

## 다음 단계

- 번들링 가이드 작성
- 컴포넌트별 상세 스타일 가이드
- 디자인 토큰 자동화

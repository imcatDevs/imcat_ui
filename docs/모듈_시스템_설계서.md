# IMCAT UI 모듈 시스템 설계서

## 📋 개요

본 문서는 IMCAT UI의 동적 모듈 로딩 시스템과 표준 모듈 설계 가이드를 다룹니다.

**핵심 원칙:**

- 필요할 때만 로드 (Lazy Loading)
- 독립적이고 재사용 가능한 모듈
- 일관된 API 인터페이스
- 자동 CSS 로딩

---

## 🏗️ 모듈 디렉토리 구조

```text
imcat-ui/
├── core/                    # 코어 (항상 로드)
│   └── ...
└── modules/                 # 선택적 모듈
    ├── modal/
    │   ├── modal.js        # 모듈 로직
    │   ├── modal.css       # 모듈 스타일
    │   └── README.md       # 모듈 문서
    ├── dropdown/
    │   ├── dropdown.js
    │   ├── dropdown.css
    │   └── README.md
    ├── tooltip/
    │   ├── tooltip.js
    │   ├── tooltip.css
    │   └── README.md
    └── ...
```

---

## 📦 모듈 카테고리

### 1. 기본 모듈 (Standard Modules)

자주 사용되는 UI 컴포넌트

- **Modal** - 모달 다이얼로그
- **Dropdown** - 드롭다운 메뉴
- **Tooltip** - 툴팁
- **Tabs** - 탭 네비게이션
- **Alert** - 알림 메시지
- **Toast** - 토스트 알림

### 2. 확장 모듈 (Extended Modules)

특수 용도의 컴포넌트

- **Form Validation** - 폼 검증
- **Ajax Wrapper** - Ajax/Fetch 래퍼
- **Animation** - 애니메이션 유틸리티
- **Chart** - 차트 (간단한)
- **DataTable** - 데이터 테이블
- **Carousel** - 캐러셀/슬라이더

### 3. 플러그인 (Plugins)

서드파티 통합

- **State Management** - 상태 관리
- **i18n** - 다국어 지원
- **Analytics** - 분석 도구 통합

---

## 🎯 모듈 표준 인터페이스

### 기본 구조

모든 모듈은 다음 구조를 따라야 합니다:

```javascript
/**
 * 모듈명
 * @module modules/moduleName
 */

class ModuleName {
  constructor(options = {}) {
    // 기본 옵션
    this.options = {
      // 기본값 정의
      ...this.defaults(),
      ...options
    };
    
    // 초기화
    this._init();
  }
  
  /**
   * 기본 옵션
   */
  defaults() {
    return {
      // 기본 옵션 값
    };
  }
  
  /**
   * 초기화
   * @private
   */
  _init() {
    // 초기화 로직
  }
  
  /**
   * Public API 메서드들
   */
  
  /**
   * 정리
   */
  destroy() {
    // 이벤트 리스너 제거
    // DOM 정리
    // 메모리 해제
  }
}

// ES6 모듈로 내보내기
export default ModuleName;
```

### 필수 메서드

모든 모듈은 다음 메서드를 구현해야 합니다:

1. **constructor(options)** - 생성자
2. **defaults()** - 기본 옵션 반환
3. **destroy()** - 정리 및 메모리 해제

### 선택적 메서드

필요에 따라 구현:

- **init()** - 명시적 초기화
- **update(options)** - 옵션 업데이트
- **reset()** - 초기 상태로 리셋

---

## 📚 표준 모듈 설계 예시

### Modal 모듈

#### modal.js

```javascript
/**
 * Modal 모듈
 * @module modules/modal
 */

import IMCAT from '../../core/index.js';

class Modal {
  constructor(options = {}) {
    this.options = {
      ...this.defaults(),
      ...options
    };
    
    this.element = null;
    this.isOpen = false;
    this._init();
  }
  
  /**
   * 기본 옵션
   */
  defaults() {
    return {
      backdrop: true,        // 배경 오버레이
      keyboard: true,        // ESC 키로 닫기
      focus: true,          // 자동 포커스
      closeButton: true,    // 닫기 버튼
      animation: true,      // 애니메이션
      onShow: null,         // 표시 시 콜백
      onHide: null          // 숨김 시 콜백
    };
  }
  
  /**
   * 초기화
   * @private
   */
  _init() {
    this._createElements();
    this._bindEvents();
  }
  
  /**
   * DOM 요소 생성
   * @private
   */
  _createElements() {
    // 모달 컨테이너
    this.element = document.createElement('div');
    this.element.className = 'imcat-modal';
    this.element.setAttribute('role', 'dialog');
    this.element.setAttribute('aria-hidden', 'true');
    
    // 배경 오버레이
    if (this.options.backdrop) {
      this.backdrop = document.createElement('div');
      this.backdrop.className = 'imcat-modal-backdrop';
    }
    
    // 모달 다이얼로그
    const dialog = document.createElement('div');
    dialog.className = 'imcat-modal-dialog';
    
    // 모달 콘텐츠
    const content = document.createElement('div');
    content.className = 'imcat-modal-content';
    
    // 헤더
    const header = document.createElement('div');
    header.className = 'imcat-modal-header';
    this.titleElement = document.createElement('h5');
    this.titleElement.className = 'imcat-modal-title';
    header.appendChild(this.titleElement);
    
    // 닫기 버튼
    if (this.options.closeButton) {
      const closeBtn = document.createElement('button');
      closeBtn.className = 'imcat-modal-close';
      closeBtn.innerHTML = '&times;';
      closeBtn.setAttribute('aria-label', 'Close');
      header.appendChild(closeBtn);
    }
    
    // 본문
    this.bodyElement = document.createElement('div');
    this.bodyElement.className = 'imcat-modal-body';
    
    // 푸터
    this.footerElement = document.createElement('div');
    this.footerElement.className = 'imcat-modal-footer';
    
    // 구조 조립
    content.appendChild(header);
    content.appendChild(this.bodyElement);
    content.appendChild(this.footerElement);
    dialog.appendChild(content);
    this.element.appendChild(dialog);
  }
  
  /**
   * 이벤트 바인딩
   * @private
   */
  _bindEvents() {
    // 닫기 버튼
    const closeBtn = this.element.querySelector('.imcat-modal-close');
    if (closeBtn) {
      closeBtn.addEventListener('click', () => this.hide());
    }
    
    // 배경 클릭
    if (this.backdrop) {
      this.backdrop.addEventListener('click', () => {
        if (this.options.backdrop !== 'static') {
          this.hide();
        }
      });
    }
    
    // ESC 키
    if (this.options.keyboard) {
      this._handleEscape = (e) => {
        if (e.key === 'Escape' && this.isOpen) {
          this.hide();
        }
      };
      document.addEventListener('keydown', this._handleEscape);
    }
  }
  
  /**
   * 모달 표시
   */
  show(title, content) {
    if (this.isOpen) return;
    
    // 타이틀 설정
    if (title) {
      IMCAT(this.titleElement).text(title);
    }
    
    // 콘텐츠 설정
    if (content) {
      if (typeof content === 'string') {
        IMCAT(this.bodyElement).html(content);
      } else {
        this.bodyElement.appendChild(content);
      }
    }
    
    // DOM에 추가
    if (this.backdrop) {
      document.body.appendChild(this.backdrop);
    }
    document.body.appendChild(this.element);
    
    // 애니메이션
    if (this.options.animation) {
      setTimeout(() => {
        this.element.classList.add('imcat-modal-show');
        if (this.backdrop) {
          this.backdrop.classList.add('imcat-modal-backdrop-show');
        }
      }, 10);
    } else {
      this.element.classList.add('imcat-modal-show');
      if (this.backdrop) {
        this.backdrop.classList.add('imcat-modal-backdrop-show');
      }
    }
    
    // 포커스
    if (this.options.focus) {
      this.element.focus();
    }
    
    this.isOpen = true;
    this.element.setAttribute('aria-hidden', 'false');
    
    // 콜백
    if (this.options.onShow) {
      this.options.onShow(this);
    }
  }
  
  /**
   * 모달 숨김
   */
  hide() {
    if (!this.isOpen) return;
    
    // 애니메이션
    this.element.classList.remove('imcat-modal-show');
    if (this.backdrop) {
      this.backdrop.classList.remove('imcat-modal-backdrop-show');
    }
    
    // DOM에서 제거
    setTimeout(() => {
      this.element.remove();
      if (this.backdrop) {
        this.backdrop.remove();
      }
    }, this.options.animation ? 300 : 0);
    
    this.isOpen = false;
    this.element.setAttribute('aria-hidden', 'true');
    
    // 콜백
    if (this.options.onHide) {
      this.options.onHide(this);
    }
  }
  
  /**
   * 토글
   */
  toggle() {
    if (this.isOpen) {
      this.hide();
    } else {
      this.show();
    }
  }
  
  /**
   * 푸터 설정
   */
  setFooter(content) {
    if (typeof content === 'string') {
      IMCAT(this.footerElement).html(content);
    } else {
      this.footerElement.appendChild(content);
    }
  }
  
  /**
   * 정리
   */
  destroy() {
    // 이벤트 리스너 제거
    if (this._handleEscape) {
      document.removeEventListener('keydown', this._handleEscape);
    }
    
    // DOM 제거
    this.element?.remove();
    this.backdrop?.remove();
    
    // 참조 해제
    this.element = null;
    this.backdrop = null;
  }
}

// 편의 메서드
Modal.alert = function(title, message) {
  const modal = new Modal();
  modal.show(title, message);
  modal.setFooter('<button class="imcat-btn" onclick="this.closest(\'.imcat-modal\').remove()">확인</button>');
  return modal;
};

Modal.confirm = function(title, message, callback) {
  const modal = new Modal();
  modal.show(title, message);
  
  const footer = document.createElement('div');
  
  const confirmBtn = document.createElement('button');
  confirmBtn.className = 'imcat-btn imcat-btn-primary';
  confirmBtn.textContent = '확인';
  confirmBtn.onclick = () => {
    callback(true);
    modal.hide();
  };
  
  const cancelBtn = document.createElement('button');
  cancelBtn.className = 'imcat-btn';
  cancelBtn.textContent = '취소';
  cancelBtn.onclick = () => {
    callback(false);
    modal.hide();
  };
  
  footer.appendChild(cancelBtn);
  footer.appendChild(confirmBtn);
  modal.setFooter(footer);
  
  return modal;
};

export default Modal;
```

#### modal.css

```css
/* Modal 스타일 */
.imcat-modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 1050;
  display: none;
  overflow: auto;
  outline: 0;
}

.imcat-modal-show {
  display: block;
}

.imcat-modal-backdrop {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5);
  z-index: 1040;
  opacity: 0;
  transition: opacity 0.3s;
}

.imcat-modal-backdrop-show {
  opacity: 1;
}

.imcat-modal-dialog {
  position: relative;
  width: auto;
  max-width: 500px;
  margin: 1.75rem auto;
  pointer-events: none;
}

.imcat-modal-content {
  position: relative;
  display: flex;
  flex-direction: column;
  width: 100%;
  pointer-events: auto;
  background-color: #fff;
  border: 1px solid rgba(0, 0, 0, 0.2);
  border-radius: 0.3rem;
  outline: 0;
}

.imcat-modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1rem;
  border-bottom: 1px solid #dee2e6;
}

.imcat-modal-title {
  margin: 0;
  font-size: 1.25rem;
  font-weight: 500;
}

.imcat-modal-close {
  padding: 0;
  background-color: transparent;
  border: 0;
  font-size: 1.5rem;
  font-weight: 700;
  line-height: 1;
  color: #000;
  opacity: 0.5;
  cursor: pointer;
}

.imcat-modal-close:hover {
  opacity: 0.75;
}

.imcat-modal-body {
  position: relative;
  flex: 1 1 auto;
  padding: 1rem;
}

.imcat-modal-footer {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  padding: 1rem;
  border-top: 1px solid #dee2e6;
}

.imcat-modal-footer > * {
  margin-left: 0.5rem;
}

/* 애니메이션 */
@keyframes modalFadeIn {
  from {
    opacity: 0;
    transform: translateY(-50px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.imcat-modal-show .imcat-modal-dialog {
  animation: modalFadeIn 0.3s;
}
```

---

## 🔧 모듈 사용 방법

### 기본 사용

```javascript
// 모듈 로드
const Modal = await IMCAT.use('modal');

// 인스턴스 생성
const modal = new Modal({
  backdrop: true,
  keyboard: true
});

// 모달 표시
modal.show('제목', '<p>내용</p>');

// 모달 숨김
modal.hide();

// 정리
modal.destroy();
```

### 편의 메서드 사용

```javascript
// Alert
const Modal = await IMCAT.use('modal');
Modal.alert('알림', '작업이 완료되었습니다.');

// Confirm
Modal.confirm('확인', '삭제하시겠습니까?', (confirmed) => {
  if (confirmed) {
    console.log('삭제됨');
  }
});
```

### 여러 모듈 동시 로드

```javascript
// 여러 모듈 한 번에 로드
const [Modal, Tooltip, Dropdown] = await IMCAT.use('modal', 'tooltip', 'dropdown');

// 사용
const modal = new Modal();
const tooltip = new Tooltip();
```

---

## 📋 모듈 개발 체크리스트

모듈을 개발할 때 확인해야 할 사항:

### 필수 사항

- [ ] **ES6 모듈 형식** - `export default`로 내보내기
- [ ] **표준 인터페이스** - constructor, defaults, destroy 구현
- [ ] **독립적 동작** - 다른 모듈에 의존하지 않음
- [ ] **CSS 파일 포함** - 모듈명.css 파일 작성
- [ ] **JSDoc 주석** - 모든 public 메서드에 주석
- [ ] **메모리 누수 방지** - destroy()에서 정리
- [ ] **이벤트 리스너 정리** - 모든 리스너 제거
- [ ] **접근성** - ARIA 속성 사용
- [ ] **보안** - XSS 방어 (IMCAT.escape/sanitize 사용)

### 권장 사항

- [ ] **옵션 검증** - 잘못된 옵션 처리
- [ ] **에러 핸들링** - try-catch 사용
- [ ] **체이닝 지원** - 메서드에서 this 반환
- [ ] **이벤트 발생** - 중요한 동작 시 이벤트 emit
- [ ] **반응형 지원** - 모바일 대응
- [ ] **테마 지원** - CSS 변수 사용
- [ ] **README 작성** - 사용법 문서화
- [ ] **예제 제공** - 간단한 사용 예제

---

## 🎨 스타일 가이드

### CSS 네이밍 규칙

```css
/* 모듈 접두사 사용 */
.imcat-{module-name} { }
.imcat-{module-name}-{element} { }
.imcat-{module-name}-{modifier} { }

/* 예시 */
.imcat-modal { }
.imcat-modal-dialog { }
.imcat-modal-backdrop { }
.imcat-modal-show { }
```

### CSS 변수 사용

```css
:root {
  --imcat-modal-bg: #fff;
  --imcat-modal-border-color: rgba(0, 0, 0, 0.2);
  --imcat-modal-border-radius: 0.3rem;
  --imcat-modal-backdrop-bg: rgba(0, 0, 0, 0.5);
}

.imcat-modal-content {
  background-color: var(--imcat-modal-bg);
  border-color: var(--imcat-modal-border-color);
  border-radius: var(--imcat-modal-border-radius);
}
```

---

## 🧪 모듈 테스트

### 단위 테스트 예시

```javascript
// modal.test.js
describe('Modal Module', () => {
  let modal;
  
  beforeEach(() => {
    modal = new Modal();
  });
  
  afterEach(() => {
    modal.destroy();
  });
  
  test('should create modal instance', () => {
    expect(modal).toBeDefined();
    expect(modal.isOpen).toBe(false);
  });
  
  test('should show modal', () => {
    modal.show('Test', 'Content');
    expect(modal.isOpen).toBe(true);
    expect(document.querySelector('.imcat-modal')).toBeTruthy();
  });
  
  test('should hide modal', () => {
    modal.show('Test', 'Content');
    modal.hide();
    expect(modal.isOpen).toBe(false);
  });
  
  test('should clean up on destroy', () => {
    modal.show('Test', 'Content');
    modal.destroy();
    expect(document.querySelector('.imcat-modal')).toBeFalsy();
  });
});
```

---

## 📊 모듈 로딩 최적화

### 코드 스플리팅

```javascript
// 조건부 로딩
if (needsAdvancedFeatures) {
  const Chart = await IMCAT.use('chart');
  // 차트 사용
}
```

### 프리로딩

```javascript
// 사용하기 전에 미리 로드
IMCAT.use('modal'); // await 없이 로드 시작

// 나중에 사용
const Modal = await IMCAT.use('modal'); // 이미 로드됨
```

### 번들링

```javascript
// 여러 모듈을 하나로 번들 (프로덕션)
import Modal from './modal.js';
import Tooltip from './tooltip.js';
import Dropdown from './dropdown.js';

export default {
  Modal,
  Tooltip,
  Dropdown
};
```

---

## 📝 다음 단계

모듈 시스템 설계가 완료되었습니다. 다음으로:

1. **API 레퍼런스 문서** - 모든 API 상세 설명
2. **모듈 개발 가이드** - 실제 모듈 구현 방법
3. **기본 모듈 구현** - Modal, Dropdown, Tooltip 등 구현

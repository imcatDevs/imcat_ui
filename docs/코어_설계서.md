# IMCAT UI 코어 아키텍처 설계서

## 📋 개요

본 문서는 IMCAT UI 프레임워크의 코어 모듈 상세 설계를 다룹니다.
코어는 항상 로드되며 프레임워크의 기반이 되는 필수 기능을 제공합니다.

**코어 크기 목표:** < 15KB (minified + gzipped)

---

## 🏗️ 코어 모듈 구조

```text
imcat-ui/core/
├── index.js        # 코어 진입점, 모든 모듈 통합
├── dom.js          # DOM 조작 유틸리티
├── event.js        # 이벤트 시스템
├── loader.js       # 동적 모듈 로더
├── router.js       # SPA 뷰 라우터 (views/ 하위 폴더 지원)
├── loading.js      # 로딩 인디케이터
├── security.js     # XSS 보안 필터
├── utils.js        # 기본 유틸리티
├── api.js          # API 유틸리티
├── template.js     # 템플릿 엔진
├── animation.js    # Web Animations API 기반 애니메이션 (20+)
├── form.js         # 폼 검증 및 처리
├── state.js        # 리액티브 상태 관리
├── storage.js      # LocalStorage/SessionStorage 래퍼
└── url.js          # URL 파싱 및 조작
```

---

## 1️⃣ Core Entry Point (index.js)

### 목적

모든 코어 모듈을 통합하고 전역 IMCAT 객체를 생성합니다.

### 설계

```javascript
/**
 * IMCAT UI 코어 진입점
 * @module core/index
 */

import { DOM } from './dom.js';
import { EventBus } from './event.js';
import { ModuleLoader } from './loader.js';
import { ViewRouter } from './router.js';
import { LoadingIndicator } from './loading.js';
import { Security } from './security.js';
import { Utils } from './utils.js';

/**
 * IMCAT 전역 객체
 */
class IMCAT {
  constructor() {
    // 코어 모듈 초기화
    this.dom = new DOM();
    this.event = new EventBus();
    this.loader = new ModuleLoader();
    this.view = new ViewRouter();
    this.loading = new LoadingIndicator();
    this.security = new Security();
    this.utils = new Utils();
    
    // 초기화
    this._init();
  }
  
  /**
   * 초기화
   * @private
   */
  _init() {
    // DOM 준비 대기
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', () => this._onReady());
    } else {
      this._onReady();
    }
  }
  
  /**
   * DOM 준비 완료 핸들러
   * @private
   */
  _onReady() {
    // 뷰 라우터 초기화
    this.view.init();
    
    // catui-href 링크 자동 바인딩
    this._bindSPALinks();
  }
  
  /**
   * SPA 링크 자동 바인딩
   * @private
   */
  _bindSPALinks() {
    document.addEventListener('click', (e) => {
      const link = e.target.closest('[catui-href]');
      if (link) {
        e.preventDefault();
        const path = link.getAttribute('catui-href');
        this.view.navigate(path);
      }
    });
  }
}

// 전역 IMCAT 인스턴스 생성
const imcat = new IMCAT();

// 선택자 함수로도 사용 가능
const IMCAT = function(selector) {
  return imcat.dom.select(selector);
};

// 코어 API 바인딩
Object.assign(IMCAT, {
  // DOM
  create: (...args) => imcat.dom.create(...args),
  
  // 모듈 로더
  use: (...args) => imcat.loader.load(...args),
  
  // 뷰 라우터
  view: imcat.view,
  
  // 로딩
  loading: imcat.loading,
  
  // 보안
  escape: (...args) => imcat.security.escape(...args),
  sanitize: (...args) => imcat.security.sanitize(...args),
  isSafeUrl: (...args) => imcat.security.isSafeUrl(...args),
  validatePath: (...args) => imcat.security.validatePath(...args),
  isSafeFilename: (...args) => imcat.security.isSafeFilename(...args),
  sanitizeParam: (...args) => imcat.security.sanitizeParam(...args),
  
  // 이벤트
  on: (...args) => imcat.event.on(...args),
  off: (...args) => imcat.event.off(...args),
  emit: (...args) => imcat.event.emit(...args),
  
  // 유틸리티
  ...imcat.utils
});

export default IMCAT;
```

### 주요 기능

1. **전역 객체 생성**
   - `IMCAT()` 함수로 DOM 선택
   - `IMCAT.use()` 로 모듈 로드
   - `IMCAT.view` 로 라우팅

2. **자동 초기화**
   - DOM 준비 완료 대기
   - SPA 링크 자동 바인딩
   - 뷰 라우터 초기화

3. **API 통합**
   - 모든 코어 기능을 IMCAT 객체에 바인딩
   - 일관된 API 제공

---

## 2️⃣ DOM Module (dom.js)

### DOM 모듈 목적

jQuery 스타일의 DOM 조작 유틸리티를 제공합니다.

### DOM 모듈 설계

```javascript
/**
 * DOM 조작 유틸리티
 * @module core/dom
 */

import { Security } from './security.js';

/**
 * DOM Element Wrapper
 */
class DOMElement {
  constructor(elements) {
    this.elements = Array.isArray(elements) ? elements : [elements];
    this.length = this.elements.length;
  }
  
  /**
   * 각 요소에 대해 함수 실행
   */
  each(callback) {
    this.elements.forEach((el, index) => callback.call(el, el, index));
    return this;
  }
  
  /**
   * 클래스 추가
   */
  addClass(className) {
    return this.each(el => el.classList.add(className));
  }
  
  /**
   * 클래스 제거
   */
  removeClass(className) {
    return this.each(el => el.classList.remove(className));
  }
  
  /**
   * 클래스 토글
   */
  toggleClass(className) {
    return this.each(el => el.classList.toggle(className));
  }
  
  /**
   * 클래스 포함 여부
   */
  hasClass(className) {
    return this.elements.some(el => el.classList.contains(className));
  }
  
  /**
   * 텍스트 설정/조회 (자동 이스케이프)
   */
  text(value) {
    if (value === undefined) {
      return this.elements[0]?.textContent || '';
    }
    // 자동 이스케이프
    const escaped = Security.escape(value);
    return this.each(el => el.textContent = escaped);
  }
  
  /**
   * HTML 설정/조회 (자동 새니타이징)
   */
  html(value) {
    if (value === undefined) {
      return this.elements[0]?.innerHTML || '';
    }
    // 자동 새니타이징
    const sanitized = Security.sanitize(value);
    return this.each(el => el.innerHTML = sanitized);
  }
  
  /**
   * 원본 HTML 설정 (새니타이징 없음)
   * 주의: 신뢰할 수 있는 소스에서만 사용!
   */
  rawHtml(value) {
    return this.each(el => el.innerHTML = value);
  }
  
  /**
   * 속성 설정/조회 (자동 이스케이프)
   */
  attr(name, value) {
    if (value === undefined) {
      return this.elements[0]?.getAttribute(name);
    }
    // 자동 이스케이프
    const escaped = Security.escape(value);
    return this.each(el => el.setAttribute(name, escaped));
  }
  
  /**
   * 속성 제거
   */
  removeAttr(name) {
    return this.each(el => el.removeAttribute(name));
  }
  
  /**
   * 데이터 속성 설정/조회
   */
  data(key, value) {
    if (value === undefined) {
      return this.elements[0]?.dataset[key];
    }
    return this.each(el => el.dataset[key] = value);
  }
  
  /**
   * CSS 스타일 설정/조회
   */
  css(property, value) {
    if (typeof property === 'object') {
      // 여러 속성 설정
      return this.each(el => {
        Object.entries(property).forEach(([prop, val]) => {
          el.style[prop] = Security.sanitizeCSS(val);
        });
      });
    }
    
    if (value === undefined) {
      return getComputedStyle(this.elements[0])[property];
    }
    
    // 위험한 CSS 값 필터링
    const sanitized = Security.sanitizeCSS(value);
    return this.each(el => el.style[property] = sanitized);
  }
  
  /**
   * 이벤트 리스너 추가
   */
  on(event, selector, handler) {
    // 인자 처리
    if (typeof selector === 'function') {
      handler = selector;
      selector = null;
    }
    
    return this.each(el => {
      if (selector) {
        // 이벤트 위임
        const delegateHandler = (e) => {
          const target = e.target.closest(selector);
          if (target && el.contains(target)) {
            handler.call(target, e);
          }
        };
        el.addEventListener(event, delegateHandler);
        // 나중에 제거할 수 있도록 저장
        if (!el._delegates) el._delegates = new Map();
        el._delegates.set(handler, delegateHandler);
      } else {
        el.addEventListener(event, handler);
      }
    });
  }
  
  /**
   * 이벤트 리스너 제거
   */
  off(event, handler) {
    return this.each(el => {
      if (el._delegates?.has(handler)) {
        el.removeEventListener(event, el._delegates.get(handler));
        el._delegates.delete(handler);
      } else {
        el.removeEventListener(event, handler);
      }
    });
  }
  
  /**
   * 보이기
   */
  show() {
    return this.each(el => el.style.display = '');
  }
  
  /**
   * 숨기기
   */
  hide() {
    return this.each(el => el.style.display = 'none');
  }
  
  /**
   * 토글
   */
  toggle() {
    return this.each(el => {
      el.style.display = el.style.display === 'none' ? '' : 'none';
    });
  }
  
  /**
   * 자식 요소 추가
   */
  append(content) {
    return this.each(el => {
      if (typeof content === 'string') {
        const sanitized = Security.sanitize(content);
        el.insertAdjacentHTML('beforeend', sanitized);
      } else if (content instanceof DOMElement) {
        content.elements.forEach(child => el.appendChild(child));
      } else if (content instanceof HTMLElement) {
        el.appendChild(content);
      }
    });
  }
  
  /**
   * 부모 요소에 추가
   */
  appendTo(parent) {
    const parentEl = typeof parent === 'string' 
      ? document.querySelector(parent) 
      : parent;
    
    if (parentEl) {
      this.elements.forEach(el => parentEl.appendChild(el));
    }
    return this;
  }
  
  /**
   * 요소 제거
   */
  remove() {
    return this.each(el => el.remove());
  }
  
  /**
   * 하위 요소 검색
   */
  find(selector) {
    const found = [];
    this.each(el => {
      found.push(...el.querySelectorAll(selector));
    });
    return new DOMElement(found);
  }
  
  /**
   * 부모 요소 검색
   */
  parent() {
    const parents = this.elements.map(el => el.parentElement).filter(Boolean);
    return new DOMElement(parents);
  }
  
  /**
   * 가장 가까운 조상 요소 검색
   */
  closest(selector) {
    const found = this.elements.map(el => el.closest(selector)).filter(Boolean);
    return new DOMElement(found);
  }
}

/**
 * DOM 유틸리티 클래스
 */
export class DOM {
  /**
   * 요소 선택
   */
  select(selector) {
    if (!selector) return new DOMElement([]);
    
    if (typeof selector === 'string') {
      const elements = Array.from(document.querySelectorAll(selector));
      return new DOMElement(elements);
    }
    
    if (selector instanceof HTMLElement) {
      return new DOMElement([selector]);
    }
    
    if (selector instanceof DOMElement) {
      return selector;
    }
    
    return new DOMElement([]);
  }
  
  /**
   * 새 요소 생성
   */
  create(tagName, attributes = {}) {
    const el = document.createElement(tagName);
    
    Object.entries(attributes).forEach(([key, value]) => {
      if (key === 'class') {
        el.className = value;
      } else if (key === 'html') {
        el.innerHTML = Security.sanitize(value);
      } else if (key === 'text') {
        el.textContent = value;
      } else {
        el.setAttribute(key, Security.escape(value));
      }
    });
    
    return new DOMElement([el]);
  }
}
```

### DOM 모듈 주요 기능

1. **자동 보안 처리**
   - `.text()` - 자동 이스케이프
   - `.html()` - 자동 새니타이징
   - `.attr()` - 속성 값 이스케이프
   - `.css()` - CSS 인젝션 방지

2. **체이닝 지원**
   - jQuery 스타일의 메서드 체이닝
   - 직관적인 API

3. **이벤트 위임**
   - 효율적인 이벤트 처리
   - 동적 요소 지원

---

## 3️⃣ Event Module (event.js)

### Event 모듈 목적

커스텀 이벤트 시스템과 이벤트 버스를 제공합니다.

### Event 모듈 설계

```javascript
/**
 * 이벤트 시스템
 * @module core/event
 */

/**
 * 이벤트 버스
 */
export class EventBus {
  constructor() {
    this.events = new Map();
  }
  
  /**
   * 이벤트 리스너 등록
   */
  on(event, handler) {
    if (!this.events.has(event)) {
      this.events.set(event, []);
    }
    this.events.get(event).push(handler);
    
    // 구독 취소 함수 반환
    return () => this.off(event, handler);
  }
  
  /**
   * 일회성 이벤트 리스너 등록
   */
  once(event, handler) {
    const wrapper = (...args) => {
      handler(...args);
      this.off(event, wrapper);
    };
    return this.on(event, wrapper);
  }
  
  /**
   * 이벤트 리스너 제거
   */
  off(event, handler) {
    if (!this.events.has(event)) return;
    
    if (handler) {
      const handlers = this.events.get(event);
      const index = handlers.indexOf(handler);
      if (index !== -1) {
        handlers.splice(index, 1);
      }
    } else {
      this.events.delete(event);
    }
  }
  
  /**
   * 이벤트 발생
   */
  emit(event, ...args) {
    if (!this.events.has(event)) return;
    
    this.events.get(event).forEach(handler => {
      try {
        handler(...args);
      } catch (error) {
        console.error(`Error in event handler for "${event}":`, error);
      }
    });
  }
  
  /**
   * 모든 리스너 제거
   */
  clear() {
    this.events.clear();
  }
}
```

### Event 모듈 주요 기능

1. **이벤트 구독/발행**
   - `on()` - 이벤트 구독
   - `once()` - 일회성 구독
   - `emit()` - 이벤트 발행
   - `off()` - 구독 취소

2. **에러 핸들링**
   - 핸들러 에러 격리
   - 콘솔 로그 출력

---

## 4️⃣ Module Loader (loader.js)

### Loader 모듈 목적

동적 모듈 로딩을 관리합니다.

### Loader 모듈 설계

```javascript
/**
 * 동적 모듈 로더
 * @module core/loader
 */

import { LoadingIndicator } from './loading.js';

/**
 * 모듈 로더
 */
export class ModuleLoader {
  constructor() {
    this.modules = new Map();
    this.loading = new Map();
    this.baseUrl = './modules/';
  }
  
  /**
   * 모듈 로드
   */
  async load(...moduleNames) {
    // 로딩 표시
    LoadingIndicator.show();
    
    try {
      const promises = moduleNames.map(name => this._loadModule(name));
      const modules = await Promise.all(promises);
      
      // 단일 모듈이면 배열이 아닌 객체 반환
      return modules.length === 1 ? modules[0] : modules;
    } finally {
      LoadingIndicator.hide();
    }
  }
  
  /**
   * 단일 모듈 로드
   * @private
   */
  async _loadModule(name) {
    // 이미 로드됨
    if (this.modules.has(name)) {
      return this.modules.get(name);
    }
    
    // 로딩 중
    if (this.loading.has(name)) {
      return this.loading.get(name);
    }
    
    // 새로 로드
    const promise = this._fetchModule(name);
    this.loading.set(name, promise);
    
    try {
      const module = await promise;
      this.modules.set(name, module);
      this.loading.delete(name);
      return module;
    } catch (error) {
      this.loading.delete(name);
      throw new Error(`Failed to load module "${name}": ${error.message}`);
    }
  }
  
  /**
   * 모듈 파일 가져오기
   * @private
   */
  async _fetchModule(name) {
    const url = `${this.baseUrl}${name}/${name}.js`;
    
    try {
      const module = await import(url);
      
      // CSS도 자동 로드
      await this._loadCSS(name);
      
      return module.default || module;
    } catch (error) {
      throw error;
    }
  }
  
  /**
   * 모듈 CSS 로드
   * @private
   */
  async _loadCSS(name) {
    const cssUrl = `${this.baseUrl}${name}/${name}.css`;
    
    // 이미 로드되었는지 확인
    if (document.querySelector(`link[href="${cssUrl}"]`)) {
      return;
    }
    
    return new Promise((resolve, reject) => {
      const link = document.createElement('link');
      link.rel = 'stylesheet';
      link.href = cssUrl;
      link.onload = resolve;
      link.onerror = () => {
        // CSS는 선택적이므로 에러 무시
        resolve();
      };
      document.head.appendChild(link);
    });
  }
  
  /**
   * 베이스 URL 설정
   */
  setBaseUrl(url) {
    this.baseUrl = url.endsWith('/') ? url : `${url}/`;
  }
}
```

### Loader 모듈 주요 기능

1. **동적 import**
   - ES6 동적 import 사용
   - 모듈 캐싱
   - 중복 로딩 방지

2. **자동 CSS 로드**
   - 모듈과 함께 CSS 자동 로드
   - 중복 로드 방지

3. **로딩 표시**
   - 모듈 로드 시 자동 로딩 표시

---

## 5️⃣ View Router (router.js)

### Router 모듈 목적

SPA 뷰 라우팅을 제공합니다. 라우트 설정 없이 파일 경로 기반으로 동작합니다.

### Router 모듈 설계

```javascript
/**
 * SPA 뷰 라우터
 * @module core/router
 */

import { Security } from './security.js';
import { LoadingIndicator } from './loading.js';

/**
 * 뷰 라우터
 */
export class ViewRouter {
  constructor() {
    this.container = '#app';
    this.currentPath = '';
    this.hooks = {
      beforeLoad: [],
      afterLoad: [],
      onError: []
    };
    // 인스턴스 관리
    this.instances = new Map();
    this.currentViewInstances = [];
  }
  
  /**
   * 초기화
   */
  init() {
    // History API 이벤트 리스너
    window.addEventListener('popstate', (e) => {
      if (e.state?.path) {
        this._loadView(e.state.path, false);
      }
    });
    
    // 초기 경로 로드
    const initialPath = window.location.hash.slice(1) || '';
    if (initialPath) {
      this.navigate(initialPath, true);
    }
  }
  
  /**
   * 페이지 이동
   */
  async navigate(path, replace = false) {
    // 경로 보안 검증
    if (!Security.validatePath(path)) {
      console.error('Invalid path:', path);
      this._emitHook('onError', new Error('Invalid path'));
      return;
    }
    
    await this._loadView(path, !replace);
  }
  
  /**
   * 뷰 로드
   * @private
   */
  async _loadView(path, pushState = true) {
    const from = this.currentPath;
    
    try {
      // 이전 뷰의 인스턴스 정리
      this._cleanupCurrentView();
      
      // beforeLoad 훅
      await this._emitHook('beforeLoad', path, from);
      
      // 로딩 표시
      LoadingIndicator.show();
      
      // HTML 파일 가져오기
      const response = await fetch(path);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
      
      const html = await response.text();
      
      // 새니타이징
      const sanitizedHtml = Security.sanitize(html);
      
      // 컨테이너에 렌더링
      const container = document.querySelector(this.container);
      if (container) {
        container.innerHTML = sanitizedHtml;
        
        // 스크립트 실행
        this._executeScripts(container);
      }
      
      // History API 업데이트
      if (pushState) {
        window.history.pushState({ path }, '', `#${path}`);
      }
      
      this.currentPath = path;
      
      // afterLoad 훅
      await this._emitHook('afterLoad', path);
      
    } catch (error) {
      console.error('Failed to load view:', error);
      await this._emitHook('onError', error);
    } finally {
      LoadingIndicator.hide();
    }
  }
  
  /**
   * 스크립트 실행
   * @private
   */
  _executeScripts(container) {
    const scripts = container.querySelectorAll('script');
    scripts.forEach(oldScript => {
      const newScript = document.createElement('script');
      
      // 속성 복사
      Array.from(oldScript.attributes).forEach(attr => {
        newScript.setAttribute(attr.name, attr.value);
      });
      
      // 내용 복사
      newScript.textContent = oldScript.textContent;
      
      // 기존 스크립트 교체
      oldScript.parentNode.replaceChild(newScript, oldScript);
    });
  }
  
  /**
   * URL 파라미터 조회
   */
  params() {
    const hash = window.location.hash.slice(1);
    const [, queryString] = hash.split('?');
    
    if (!queryString) return {};
    
    const params = {};
    const searchParams = new URLSearchParams(queryString);
    
    for (const [key, value] of searchParams) {
      // 자동 이스케이프
      params[key] = Security.escape(value);
    }
    
    return params;
  }
  
  /**
   * 뒤로 가기
   */
  back() {
    window.history.back();
  }
  
  /**
   * 앞으로 가기
   */
  forward() {
    window.history.forward();
  }
  
  /**
   * 현재 경로
   */
  current() {
    return this.currentPath;
  }
  
  /**
   * 컨테이너 설정
   */
  setContainer(selector) {
    this.container = selector;
  }
  
  /**
   * 인스턴스 등록 (메모리 누수 방지)
   */
  registerInstance(instance) {
    if (instance && typeof instance.destroy === 'function') {
      this.currentViewInstances.push(instance);
      return instance;
    }
    console.warn('Instance must have destroy() method');
    return instance;
  }
  
  /**
   * 현재 뷰의 모든 인스턴스 정리
   * @private
   */
  _cleanupCurrentView() {
    // 현재 뷰의 모든 인스턴스 정리
    this.currentViewInstances.forEach(instance => {
      try {
        if (instance && typeof instance.destroy === 'function') {
          instance.destroy();
        }
      } catch (error) {
        console.error('Error destroying instance:', error);
      }
    });
    
    // 인스턴스 배열 초기화
    this.currentViewInstances = [];
  }
  
  /**
   * beforeLoad 훅 등록
   */
  beforeLoad(handler) {
    this.hooks.beforeLoad.push(handler);
    return () => this._removeHook('beforeLoad', handler);
  }
  
  /**
   * afterLoad 훅 등록
   */
  afterLoad(handler) {
    this.hooks.afterLoad.push(handler);
    return () => this._removeHook('afterLoad', handler);
  }
  
  /**
   * onError 훅 등록
   */
  onError(handler) {
    this.hooks.onError.push(handler);
    return () => this._removeHook('onError', handler);
  }
  
  /**
   * 훅 실행
   * @private
   */
  async _emitHook(name, ...args) {
    const handlers = this.hooks[name] || [];
    for (const handler of handlers) {
      try {
        await handler(...args);
      } catch (error) {
        console.error(`Error in ${name} hook:`, error);
      }
    }
  }
  
  /**
   * 훅 제거
   * @private
   */
  _removeHook(name, handler) {
    const handlers = this.hooks[name];
    const index = handlers.indexOf(handler);
    if (index !== -1) {
      handlers.splice(index, 1);
    }
  }
}
```

### 주요 기능 (Router)

1. **파일 경로 기반 라우팅**
   - 라우트 설정 불필요
   - 파일 경로를 직접 사용
   - 자동 보안 검증

2. **인스턴스 관리 (메모리 누수 방지)**
   - 뷰 전환 시 자동 인스턴스 정리
   - destroy() 메서드 자동 호출
   - 메모리 누수 방지

3. **라이프사이클 훅**
   - beforeLoad
   - afterLoad
   - onError

4. **History API**
   - pushState로 URL 관리
   - 뒤로/앞으로 가기 지원

5. **URL 파라미터**
   - 쿼리 문자열 파싱
   - 자동 이스케이프

---

## 6️⃣ Loading Indicator (loading.js)

### Loading 모듈 목적

페이지 및 모듈 로딩 시 로딩 인디케이터를 표시합니다.

### Loading 모듈 설계

```javascript
/**
 * 로딩 인디케이터
 * @module core/loading
 */

/**
 * 로딩 인디케이터 클래스
 */
export class LoadingIndicator {
  constructor() {
    this.element = null;
    this.config = {
      style: 'spinner', // 'spinner', 'bar', 'dots'
      color: '#007bff',
      position: 'center', // 'center', 'top'
      delay: 200 // ms
    };
    this.showTimer = null;
    this.hideTimer = null;
    this.isShowing = false;
  }
  
  /**
   * 로딩 표시
   */
  show(message = '') {
    // 이미 표시 중이면 무시
    if (this.isShowing) return;
    
    // 기존 타이머 취소
    if (this.hideTimer) {
      clearTimeout(this.hideTimer);
      this.hideTimer = null;
    }
    
    // delay 후에 표시 (빠른 로딩은 표시 안함)
    this.showTimer = setTimeout(() => {
      this._createElement(message);
      this._show();
      this.isShowing = true;
    }, this.config.delay);
  }
  
  /**
   * 로딩 숨김
   */
  hide() {
    // 타이머 취소
    if (this.showTimer) {
      clearTimeout(this.showTimer);
      this.showTimer = null;
    }
    
    if (!this.isShowing) return;
    
    // 부드러운 페이드아웃
    this.hideTimer = setTimeout(() => {
      this._hide();
      this.isShowing = false;
    }, 100);
  }
  
  /**
   * 진행률 설정 (프로그레스 바)
   */
  progress(percent) {
    if (!this.element) return;
    
    const bar = this.element.querySelector('.imcat-loading-bar-fill');
    if (bar) {
      bar.style.width = `${Math.max(0, Math.min(100, percent))}%`;
    }
  }
  
  /**
   * 설정 변경
   */
  config(options) {
    Object.assign(this.config, options);
  }
  
  /**
   * 엘리먼트 생성
   * @private
   */
  _createElement(message) {
    if (this.element) return;
    
    this.element = document.createElement('div');
    this.element.className = `imcat-loading imcat-loading-${this.config.position}`;
    
    let innerHTML = '';
    
    if (this.config.style === 'spinner') {
      innerHTML = `
        <div class="imcat-loading-spinner">
          <div class="imcat-spinner"></div>
          ${message ? `<div class="imcat-loading-message">${message}</div>` : ''}
        </div>
      `;
    } else if (this.config.style === 'bar') {
      innerHTML = `
        <div class="imcat-loading-bar">
          <div class="imcat-loading-bar-fill"></div>
        </div>
      `;
    } else if (this.config.style === 'dots') {
      innerHTML = `
        <div class="imcat-loading-dots">
          <span></span><span></span><span></span>
        </div>
      `;
    }
    
    this.element.innerHTML = innerHTML;
    
    // CSS 변수 적용
    this.element.style.setProperty('--imcat-loading-color', this.config.color);
  }
  
  /**
   * 표시
   * @private
   */
  _show() {
    if (!this.element) return;
    
    document.body.appendChild(this.element);
    
    // 애니메이션을 위한 reflow
    this.element.offsetHeight;
    
    this.element.classList.add('imcat-loading-show');
  }
  
  /**
   * 숨김
   * @private
   */
  _hide() {
    if (!this.element) return;
    
    this.element.classList.remove('imcat-loading-show');
    
    setTimeout(() => {
      this.element?.remove();
      this.element = null;
    }, 300);
  }
}

// 싱글톤 인스턴스
export default new LoadingIndicator();
```

### 주요 기능 (Loading)

1. **다양한 스타일**
   - 스피너
   - 프로그레스 바
   - 도트 애니메이션

2. **지연 표시**
   - 빠른 로딩은 표시 안함
   - 사용자 경험 개선

3. **진행률 표시**
   - 프로그레스 바 업데이트

---

## 7️⃣ Security Module (security.js)

### Security 모듈 목적

XSS 공격 방어 및 보안 검증을 제공합니다.

### Security 모듈 설계

```javascript
/**
 * XSS 보안 필터
 * @module core/security
 */

/**
 * 보안 유틸리티
 */
export class Security {
  /**
   * HTML 이스케이프
   */
  static escape(str) {
    if (typeof str !== 'string') return str;
    
    const map = {
      '&': '&amp;',
      '<': '&lt;',
      '>': '&gt;',
      '"': '&quot;',
      "'": '&#x27;',
      '/': '&#x2F;'
    };
    
    return str.replace(/[&<>"'/]/g, char => map[char]);
  }
  
  /**
   * HTML 새니타이징
   */
  static sanitize(html) {
    if (typeof html !== 'string') return '';
    
    // DOMParser로 파싱
    const parser = new DOMParser();
    const doc = parser.parseFromString(html, 'text/html');
    
    // 위험한 요소 제거
    this._removeDangerousElements(doc.body);
    
    // 위험한 속성 제거
    this._removeDangerousAttributes(doc.body);
    
    return doc.body.innerHTML;
  }
  
  /**
   * 위험한 요소 제거
   * @private
   */
  static _removeDangerousElements(element) {
    const dangerous = ['script', 'iframe', 'object', 'embed', 'link', 'style'];
    
    dangerous.forEach(tag => {
      const elements = element.querySelectorAll(tag);
      elements.forEach(el => el.remove());
    });
  }
  
  /**
   * 위험한 속성 제거
   * @private
   */
  static _removeDangerousAttributes(element) {
    const all = element.querySelectorAll('*');
    
    all.forEach(el => {
      // on* 이벤트 핸들러 제거
      Array.from(el.attributes).forEach(attr => {
        if (attr.name.startsWith('on')) {
          el.removeAttribute(attr.name);
        }
        
        // javascript: URL 제거
        if (attr.value && attr.value.trim().toLowerCase().startsWith('javascript:')) {
          el.removeAttribute(attr.name);
        }
        
        // data: URL (일부)
        if (attr.name === 'src' || attr.name === 'href') {
          if (attr.value && attr.value.trim().toLowerCase().startsWith('data:')) {
            // 이미지 data URL만 허용
            if (!attr.value.match(/^data:image\/(png|jpg|jpeg|gif|svg\+xml);base64,/i)) {
              el.removeAttribute(attr.name);
            }
          }
        }
      });
    });
  }
  
  /**
   * CSS 값 새니타이징
   */
  static sanitizeCSS(value) {
    if (typeof value !== 'string') return value;
    
    // javascript:, data: URL 제거
    if (value.match(/javascript:|data:/i)) {
      return '';
    }
    
    // expression() 제거 (IE)
    if (value.match(/expression\s*\(/i)) {
      return '';
    }
    
    return value;
  }
  
  /**
   * URL 안전성 검증
   */
  static isSafeUrl(url) {
    if (typeof url !== 'string') return false;
    
    const trimmed = url.trim().toLowerCase();
    
    // 위험한 프로토콜
    const dangerous = ['javascript:', 'data:', 'vbscript:', 'file:'];
    
    return !dangerous.some(proto => trimmed.startsWith(proto));
  }
  
  /**
   * 파일 경로 검증
   */
  static validatePath(path) {
    if (typeof path !== 'string') return false;
    
    // 경로 순회 방지
    if (path.includes('../') || path.includes('..\\')) {
      return false;
    }
    
    // 절대 경로 금지 (views/로 시작해야 함)
    if (path.startsWith('/') || path.match(/^[a-z]:/i)) {
      return false;
    }
    
    // null byte 방지
    if (path.includes('\0') || path.includes('%00')) {
      return false;
    }
    
    // URL 인코딩 우회 방지
    const decoded = decodeURIComponent(path);
    if (decoded.includes('../') || decoded.includes('..\\')) {
      return false;
    }
    
    // 허용된 확장자만
    const ext = path.split('.').pop().toLowerCase();
    if (!['html', 'php'].includes(ext)) {
      return false;
    }
    
    // 허용된 문자만
    if (!path.match(/^[a-z0-9\/\-_\.?&=]+$/i)) {
      return false;
    }
    
    return true;
  }
  
  /**
   * 파일명 검증
   */
  static isSafeFilename(filename) {
    if (typeof filename !== 'string') return false;
    
    // 경로 순회 방지
    if (filename.includes('/') || filename.includes('\\')) {
      return false;
    }
    
    // 특수 문자 검증
    return /^[a-z0-9\-_\.]+$/i.test(filename);
  }
  
  /**
   * URL 파라미터 새니타이징
   */
  static sanitizeParam(value) {
    if (typeof value !== 'string') return value;
    
    // SQL 인젝션 패턴 제거
    let sanitized = value.replace(/['";\\]/g, '');
    
    // HTML 이스케이프
    sanitized = this.escape(sanitized);
    
    return sanitized;
  }
}
```

### 주요 기능 (Security)

1. **HTML 보안**
   - escape() - 텍스트 이스케이프
   - sanitize() - HTML 새니타이징
   - 위험한 태그/속성 자동 제거

2. **경로 검증**
   - validatePath() - 경로 순회 방지
   - isSafeFilename() - 파일명 검증
   - URL 인코딩 우회 방지

3. **인젝션 방어**
   - CSS 인젝션 방지
   - URL 검증
   - 파라미터 새니타이징

---

## 8️⃣ API Module (api.js)

### API 모듈 목적

JSON 통일 응답형식 및 HTTP 요청 래퍼를 제공하여 일관된 API 통신을 보장합니다.

### API 모듈 설계

```javascript
/**
 * API 유틸리티
 * @module core/api
 */

/**
 * @typedef {Object} ApiResponse
 * @property {boolean} success - 성공 여부
 * @property {number} statusCode - HTTP 상태 코드
 * @property {*} data - 응답 데이터
 * @property {string|null} message - 메시지
 * @property {Object|null} error - 에러 정보
 * @property {number} timestamp - 타임스탬프
 */

/**
 * API 유틸리티 클래스
 */
export class APIUtil {
  /**
   * 성공 응답 생성
   * @param {*} data - 응답 데이터
   * @param {string} message - 성공 메시지
   * @param {number} statusCode - 상태 코드 (기본: 200)
   * @returns {ApiResponse}
   */
  static success(data, message = 'Success', statusCode = 200) {
    return {
      success: true,
      statusCode,
      data,
      message,
      error: null,
      timestamp: Date.now()
    };
  }

  /**
   * 에러 응답 생성
   * @param {string} message - 에러 메시지
   * @param {number} statusCode - 상태 코드 (기본: 400)
   * @param {Object} error - 에러 상세
   * @returns {ApiResponse}
   */
  static error(message, statusCode = 400, error = null) {
    return {
      success: false,
      statusCode,
      data: null,
      message,
      error: error || { message },
      timestamp: Date.now()
    };
  }

  /**
   * 페이지네이션 응답 생성
   * @param {Array} items - 항목 목록
   * @param {Object} pagination - 페이지 정보
   * @returns {ApiResponse}
   */
  static paginated(items, pagination) {
    return this.success({
      items,
      pagination: {
        page: pagination.page || 1,
        limit: pagination.limit || 10,
        total: pagination.total || items.length,
        totalPages: Math.ceil((pagination.total || items.length) / (pagination.limit || 10))
      }
    });
  }

  /**
   * fetch 래퍼 - 자동 JSON 파싱 및 에러 처리
   * @param {string} url - 요청 URL
   * @param {Object} options - fetch 옵션
   * @returns {Promise<ApiResponse>}
   */
  static async request(url, options = {}) {
    try {
      const response = await fetch(url, {
        ...options,
        headers: {
          'Content-Type': 'application/json',
          ...options.headers
        }
      });

      // JSON 파싱 시도
      let data;
      const contentType = response.headers.get('content-type');
      if (contentType && contentType.includes('application/json')) {
        data = await response.json();
      } else {
        data = await response.text();
      }

      if (!response.ok) {
        return this.error(
          data.message || `HTTP ${response.status}: ${response.statusText}`,
          response.status,
          typeof data === 'object' ? data.error : { message: data }
        );
      }

      // 이미 표준 포맷인지 확인
      if (data && typeof data === 'object' && 
          data.success !== undefined && data.statusCode !== undefined) {
        return data;
      }

      // 표준 포맷으로 변환
      return this.success(data, 'Success', response.status);
    } catch (error) {
      return this.error(
        error.message || 'Network error',
        0,
        { 
          name: error.name, 
          message: error.message,
          type: 'NetworkError'
        }
      );
    }
  }

  /**
   * GET 요청
   * @param {string} url - 요청 URL
   * @param {Object} options - fetch 옵션
   * @returns {Promise<ApiResponse>}
   */
  static async get(url, options = {}) {
    return this.request(url, { ...options, method: 'GET' });
  }

  /**
   * POST 요청
   * @param {string} url - 요청 URL
   * @param {*} body - 요청 바디
   * @param {Object} options - fetch 옵션
   * @returns {Promise<ApiResponse>}
   */
  static async post(url, body, options = {}) {
    return this.request(url, {
      ...options,
      method: 'POST',
      body: JSON.stringify(body)
    });
  }

  /**
   * PUT 요청
   * @param {string} url - 요청 URL
   * @param {*} body - 요청 바디
   * @param {Object} options - fetch 옵션
   * @returns {Promise<ApiResponse>}
   */
  static async put(url, body, options = {}) {
    return this.request(url, {
      ...options,
      method: 'PUT',
      body: JSON.stringify(body)
    });
  }

  /**
   * PATCH 요청
   * @param {string} url - 요청 URL
   * @param {*} body - 요청 바디
   * @param {Object} options - fetch 옵션
   * @returns {Promise<ApiResponse>}
   */
  static async patch(url, body, options = {}) {
    return this.request(url, {
      ...options,
      method: 'PATCH',
      body: JSON.stringify(body)
    });
  }

  /**
   * DELETE 요청
   * @param {string} url - 요청 URL
   * @param {Object} options - fetch 옵션
   * @returns {Promise<ApiResponse>}
   */
  static async delete(url, options = {}) {
    return this.request(url, { ...options, method: 'DELETE' });
  }

  /**
   * 여러 요청을 병렬로 실행
   * @param {Array<Promise>} requests - 요청 프로미스 배열
   * @returns {Promise<Array<ApiResponse>>}
   */
  static async all(...requests) {
    return Promise.all(requests);
  }

  /**
   * 여러 요청 중 가장 빠른 것만 반환
   * @param {Array<Promise>} requests - 요청 프로미스 배열
   * @returns {Promise<ApiResponse>}
   */
  static async race(...requests) {
    return Promise.race(requests);
  }
}

// 전역 API 유틸리티로 내보내기
export default APIUtil;
```

### 주요 기능 (API)

1. **표준 응답 포맷**
   - success - 성공 응답 생성
   - error - 에러 응답 생성
   - paginated - 페이지네이션 응답 생성
   - 모든 응답에 timestamp 포함

2. **HTTP 메서드 래퍼**
   - GET, POST, PUT, PATCH, DELETE 지원
   - 자동 JSON 파싱
   - Content-Type 헤더 자동 설정
   - 커스텀 헤더 지원

3. **에러 처리**
   - HTTP 에러 자동 감지
   - 네트워크 에러 처리
   - 일관된 에러 포맷
   - 상태 코드 포함

4. **병렬 요청**
   - Promise.all 래퍼
   - Promise.race 래퍼
   - 여러 API 동시 호출 지원

### 사용 예시

```javascript
// GET 요청
const users = await IMCAT.api.get('/api/users');
if (users.success) {
  console.log(users.data);
}

// POST 요청
const newUser = await IMCAT.api.post('/api/users', {
  name: 'John Doe',
  email: 'john@example.com'
});

// 커스텀 헤더
const token = localStorage.getItem('token');
const profile = await IMCAT.api.get('/api/profile', {
  headers: {
    'Authorization': `Bearer ${token}`
  }
});

// 에러 처리
const response = await IMCAT.api.get('/api/data');
if (!response.success) {
  if (response.statusCode === 404) {
    console.log('데이터를 찾을 수 없습니다');
  } else if (response.statusCode === 0) {
    console.log('네트워크 오류');
  } else {
    console.error(response.error);
  }
}

// 병렬 요청
const [users, posts, comments] = await IMCAT.api.all(
  IMCAT.api.get('/api/users'),
  IMCAT.api.get('/api/posts'),
  IMCAT.api.get('/api/comments')
);

// 페이지네이션
const page1 = await IMCAT.api.get('/api/users?page=1&limit=10');
console.log(page1.data.pagination); // { page: 1, limit: 10, total: 100, totalPages: 10 }
```

### 응답 포맷 상세

```typescript
interface ApiResponse {
  success: boolean;        // 성공 여부
  statusCode: number;      // HTTP 상태 코드 (0 = 네트워크 오류)
  data: any;              // 응답 데이터 (성공 시)
  message: string | null; // 메시지
  error: {                // 에러 정보 (실패 시)
    message: string;
    name?: string;
    type?: string;
  } | null;
  timestamp: number;      // Unix 타임스탬프
}
```

### 보안 고려사항

1. **CSRF 방어**
   - 필요시 CSRF 토큰을 헤더에 추가
   - 쿠키 기반 인증 시 `credentials: 'include'` 설정

2. **인증 토큰**
   - Authorization 헤더로 전송
   - localStorage/sessionStorage 사용 권장

3. **HTTPS 사용**
   - 프로덕션 환경에서는 HTTPS 필수
   - 민감한 데이터 전송 시 암호화

---

## 9️⃣ Utils Module (utils.js)

### Utils 모듈 목적

기본 유틸리티 함수를 제공합니다.

### Utils 모듈 설계

```javascript
/**
 * 기본 유틸리티
 * @module core/utils
 */

export class Utils {
  /**
   * 타입 체크
   */
  static isString(value) {
    return typeof value === 'string';
  }
  
  static isNumber(value) {
    return typeof value === 'number' && !isNaN(value);
  }
  
  static isBoolean(value) {
    return typeof value === 'boolean';
  }
  
  static isObject(value) {
    return value !== null && typeof value === 'object' && !Array.isArray(value);
  }
  
  static isArray(value) {
    return Array.isArray(value);
  }
  
  static isFunction(value) {
    return typeof value === 'function';
  }
  
  static isNull(value) {
    return value === null;
  }
  
  static isUndefined(value) {
    return value === undefined;
  }
  
  static isNullOrUndefined(value) {
    return value === null || value === undefined;
  }
  
  /**
   * 객체 조작
   */
  static extend(target, ...sources) {
    sources.forEach(source => {
      if (this.isObject(source)) {
        Object.keys(source).forEach(key => {
          if (this.isObject(source[key]) && this.isObject(target[key])) {
            target[key] = this.extend({}, target[key], source[key]);
          } else {
            target[key] = source[key];
          }
        });
      }
    });
    return target;
  }
  
  static clone(obj) {
    if (!this.isObject(obj) && !this.isArray(obj)) {
      return obj;
    }
    
    return JSON.parse(JSON.stringify(obj));
  }
  
  /**
   * 문자열 조작
   */
  static camelCase(str) {
    return str.replace(/[-_](\w)/g, (_, c) => c.toUpperCase());
  }
  
  static kebabCase(str) {
    return str.replace(/([a-z])([A-Z])/g, '$1-$2').toLowerCase();
  }
  
  static capitalize(str) {
    return str.charAt(0).toUpperCase() + str.slice(1);
  }
  
  /**
   * 배열 조작
   */
  static unique(array) {
    return [...new Set(array)];
  }
  
  static flatten(array) {
    return array.reduce((acc, val) => 
      Array.isArray(val) ? acc.concat(this.flatten(val)) : acc.concat(val), []);
  }
  
  /**
   * 함수 조작
   */
  static debounce(func, wait) {
    let timeout;
    return function executedFunction(...args) {
      const later = () => {
        clearTimeout(timeout);
        func(...args);
      };
      clearTimeout(timeout);
      timeout = setTimeout(later, wait);
    };
  }
  
  static throttle(func, limit) {
    let inThrottle;
    return function(...args) {
      if (!inThrottle) {
        func.apply(this, args);
        inThrottle = true;
        setTimeout(() => inThrottle = false, limit);
      }
    };
  }
  
  /**
   * 랜덤
   */
  static randomId(prefix = 'id') {
    return `${prefix}-${Math.random().toString(36).substr(2, 9)}`;
  }
  
  static randomInt(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
  }
}

// 전역 유틸리티로 내보내기
export default Utils;
```

### 주요 기능 (Utils)

1. **타입 체크**
   - 모든 기본 타입 검증
   - null/undefined 체크

2. **객체/배열 조작**
   - extend - 객체 병합
   - clone - 깊은 복사
   - unique - 중복 제거
   - flatten - 배열 평탄화

3. **함수 유틸리티**
   - debounce - 디바운스
   - throttle - 스로틀

---

## 🔟 Template Module (template.js)

### Template 모듈 목적

간단하고 빠른 템플릿 렌더링을 제공합니다. 복잡한 템플릿 엔진 없이도 동적 HTML을 안전하게 생성할 수 있습니다.

### Template 모듈 설계

```javascript
/**
 * 템플릿 엔진
 * @module core/template
 */

export class Template {
  /**
   * 템플릿 렌더링
   * @param {string} template - 템플릿 문자열
   * @param {Object} data - 데이터 객체
   * @returns {string} 렌더링된 HTML
   * 
   * @example
   * const html = Template.render('Hello {{name}}!', { name: 'John' });
   * // 'Hello John!'
   */
  static render(template, data = {}) {
    // {{key}} 형식의 플레이스홀더를 데이터로 치환
    return template.replace(/\{\{(\w+)\}\}/g, (match, key) => {
      const value = data[key];
      // undefined/null은 빈 문자열로
      if (value === undefined || value === null) {
        return '';
      }
      // 자동 이스케이프 (XSS 방어)
      return this._escape(String(value));
    });
  }

  /**
   * 안전하지 않은 렌더링 (이스케이프 없음)
   * 신뢰할 수 있는 HTML만 사용!
   * @param {string} template - 템플릿 문자열
   * @param {Object} data - 데이터 객체
   * @returns {string} 렌더링된 HTML
   */
  static renderRaw(template, data = {}) {
    return template.replace(/\{\{(\w+)\}\}/g, (match, key) => {
      const value = data[key];
      return value !== undefined && value !== null ? String(value) : '';
    });
  }

  /**
   * 조건부 렌더링
   * @param {boolean} condition - 조건
   * @param {string} template - 템플릿 문자열
   * @param {Object} data - 데이터 객체
   * @returns {string} 렌더링된 HTML 또는 빈 문자열
   * 
   * @example
   * Template.if(user.isAdmin, '<button>Admin Panel</button>', user);
   */
  static if(condition, template, data = {}) {
    return condition ? this.render(template, data) : '';
  }

  /**
   * 리스트 렌더링
   * @param {Array} items - 아이템 배열
   * @param {string} template - 각 아이템의 템플릿
   * @returns {string} 렌더링된 HTML
   * 
   * @example
   * Template.each(users, '<li>{{name}} ({{age}})</li>');
   */
  static each(items, template) {
    if (!Array.isArray(items) || items.length === 0) {
      return '';
    }
    return items.map(item => this.render(template, item)).join('');
  }

  /**
   * 템플릿 컴파일 (재사용을 위한 함수 생성)
   * @param {string} template - 템플릿 문자열
   * @returns {Function} 렌더링 함수
   * 
   * @example
   * const greeting = Template.compile('Hello {{name}}!');
   * greeting({ name: 'John' }); // 'Hello John!'
   * greeting({ name: 'Jane' }); // 'Hello Jane!'
   */
  static compile(template) {
    return (data = {}) => this.render(template, data);
  }

  /**
   * HTML 이스케이프 (내부용)
   * @private
   */
  static _escape(str) {
    const div = document.createElement('div');
    div.textContent = str;
    return div.innerHTML;
  }
}

export default Template;
```

### Template 모듈 주요 기능

#### 1. 기본 렌더링

```javascript
const template = 'Hello {{name}}, you are {{age}} years old.';
const data = { name: 'John', age: 30 };

const html = Template.render(template, data);
// 'Hello John, you are 30 years old.'
```

#### 2. 자동 XSS 방어

```javascript
const data = { 
  name: '<script>alert("XSS")</script>' 
};

const html = Template.render('Hello {{name}}!', data);
// 'Hello &lt;script&gt;alert("XSS")&lt;/script&gt;!'
```

#### 3. 조건부 렌더링

```javascript
const user = { isAdmin: true, name: 'Admin' };

const html = Template.if(
  user.isAdmin,
  '<div class="admin-panel">Welcome {{name}}</div>',
  user
);
```

#### 4. 리스트 렌더링

```javascript
const users = [
  { name: 'John', age: 30 },
  { name: 'Jane', age: 25 }
];

const html = Template.each(
  users,
  '<li>{{name}} is {{age}} years old</li>'
);
// '<li>John is 30 years old</li><li>Jane is 25 years old</li>'
```

#### 5. 템플릿 컴파일

```javascript
// 템플릿을 한 번만 컴파일하고 여러 번 재사용
const userCard = Template.compile(`
  <div class="card">
    <h3>{{name}}</h3>
    <p>{{email}}</p>
  </div>
`);

const html1 = userCard({ name: 'John', email: 'john@example.com' });
const html2 = userCard({ name: 'Jane', email: 'jane@example.com' });
```

### Template 모듈 사용 예시

#### 사용자 목록 렌더링

```javascript
// 데이터
const users = [
  { id: 1, name: 'John Doe', role: 'Admin' },
  { id: 2, name: 'Jane Smith', role: 'User' }
];

// 템플릿
const listTemplate = `
  <ul class="user-list">
    ${Template.each(users, `
      <li data-id="{{id}}">
        <strong>{{name}}</strong> - {{role}}
      </li>
    `)}
  </ul>
`;

// DOM에 삽입
IMCAT('#app').html(listTemplate);
```

#### 동적 카드 생성

```javascript
function renderProductCard(product) {
  return Template.render(`
    <div class="product-card">
      <h3>{{name}}</h3>
      <p class="price">₩{{price}}</p>
      ${Template.if(product.inStock, '<span class="badge">재고 있음</span>')}
    </div>
  `, product);
}

const product = { 
  name: 'MacBook Pro', 
  price: '2,500,000',
  inStock: true 
};

IMCAT('#products').append(renderProductCard(product));
```

### Template 모듈 보안

#### XSS 방어

- 기본적으로 모든 값을 자동 이스케이프
- `renderRaw()`는 신뢰할 수 있는 HTML만 사용
- Security 모듈과 통합

```javascript
// 안전 - 자동 이스케이프
Template.render('{{userInput}}', { userInput: '<script>xss</script>' });

// 위험 - 이스케이프 없음 (관리자 콘텐츠만 사용)
Template.renderRaw('{{adminContent}}', { adminContent: '<b>Bold</b>' });
```

### Template 모듈 성능

- **경량:** ~2KB (minified)
- **빠름:** 정규표현식 기반, 컴파일 캐싱
- **간단:** 복잡한 로직 없음, 학습 곡선 낮음

---

## 1️⃣1️⃣ Animation Module (animation.js)

### Animation 모듈 목적

Web Animations API 기반의 부드럽고 성능 좋은 애니메이션을 제공합니다.

### Animation 모듈 설계

```javascript
/**
 * 애니메이션 유틸리티
 * @module core/animation
 */

export class AnimationUtil {
  /**
   * 애니메이터 생성
   * @param {HTMLElement|string} element - 대상 요소
   * @returns {Animator}
   */
  static animate(element) {
    return new Animator(element);
  }
}

class Animator {
  constructor(element) {
    this.element = typeof element === 'string' 
      ? document.querySelector(element) 
      : element;
  }

  // Fade 애니메이션
  fadeIn(duration = 300, easing = 'ease-out') { ... }
  fadeOut(duration = 300, easing = 'ease-in') { ... }
  
  // Scale 애니메이션
  scaleIn(duration = 300, easing = 'ease-out') { ... }
  scaleOut(duration = 300, easing = 'ease-in') { ... }
  
  // Bounce 애니메이션
  bounceIn(duration = 600, easing = 'cubic-bezier(...)') { ... }
  bounceOut(duration = 600, easing = 'cubic-bezier(...)') { ... }
  
  // 효과 애니메이션 (Web Animations API)
  shake(duration = 500) { ... }
  pulse(duration = 500) { ... }
  tada(duration = 800) { ... }
  swing(duration = 800) { ... }
  wobble(duration = 800) { ... }
  // ... 20+ 애니메이션
}
```

### 주요 기능

1. **Web Animations API 기반**
   - GPU 가속
   - 부드러운 60fps 애니메이션
   - 네이티브 브라우저 성능

2. **다양한 애니메이션 (20+)**
   - Fade (In/Out)
   - Scale (In/Out)
   - Bounce (In/Out)
   - Slide (Down/Up)
   - Rotate (In/Out)
   - Flip (In/Out)
   - Shake, Pulse, Tada, Swing, Wobble, etc.

3. **사용 예시**

```javascript
// 페이드 인
await IMCAT.animate('#card').fadeIn(600);

// 바운스 인
await IMCAT.animate('.button').bounceIn(500);

// 흔들기 효과
await IMCAT.animate('#alert').shake();
```

---

## 1️⃣2️⃣ Form Module (form.js)

### Form 모듈 목적

폼 검증 및 처리를 간편하게 제공합니다.

### 주요 기능

1. **검증 규칙**
   - required, email, url, number, min, max, pattern 등

2. **자동 검증**
   - 실시간 검증
   - submit 이벤트 검증
   - 커스텀 검증 규칙

3. **사용 예시**

```javascript
const validator = IMCAT.form.validate('#myForm', {
  rules: {
    email: ['required', 'email'],
    password: ['required', { min: 8 }]
  }
});
```

---

## 1️⃣3️⃣ State Module (state.js)

### State 모듈 목적

리액티브 상태 관리를 제공합니다.

### 주요 기능

1. **리액티브 상태**
   - 상태 변경 시 자동 UI 업데이트
   - watch, computed 지원

2. **전역 상태**
   - 앱 전체에서 공유하는 상태
   - 구독/발행 패턴

3. **사용 예시**

```javascript
const state = IMCAT.state.create({
  count: 0,
  user: null
});

state.watch('count', (newValue) => {
  console.log('Count changed:', newValue);
});

state.set('count', 5); // 자동으로 watch 콜백 실행
```

---

## 1️⃣4️⃣ Storage Module (storage.js)

### Storage 모듈 목적

localStorage/sessionStorage를 편리하게 사용할 수 있는 래퍼를 제공합니다.

### 주요 기능

1. **자동 직렬화/역직렬화**
   - JSON 자동 변환
   - 타입 안전성

2. **만료 시간 지원**
   - TTL (Time To Live) 설정
   - 자동 만료 처리

3. **사용 예시**

```javascript
// 저장
IMCAT.storage.set('user', { name: 'John', age: 30 });

// 조회
const user = IMCAT.storage.get('user');

// 만료 시간 설정 (5분)
IMCAT.storage.set('token', 'abc123', { ttl: 300 });
```

---

## 1️⃣5️⃣ URL Module (url.js)

### URL 모듈 목적

URL 파싱 및 조작을 쉽게 만듭니다.

### 주요 기능

1. **쿼리 스트링 파싱**
   - URLSearchParams 래퍼
   - 객체 ↔ 쿼리 스트링 변환

2. **URL 빌더**
   - URL 생성 및 수정
   - 경로 조합

3. **사용 예시**

```javascript
// 쿼리 파싱
const params = IMCAT.url.parse('?id=1&name=John');
// { id: '1', name: 'John' }

// 쿼리 생성
const query = IMCAT.url.stringify({ id: 1, name: 'John' });
// 'id=1&name=John'
```

---

## 📐 코어 통합 흐름

```text
1. 사용자가 IMCAT() 호출
   ↓
2. Core Index가 모든 모듈 초기화
   ↓
3. DOM Ready 이벤트 감지
   ↓
4. ViewRouter 초기화
   ↓
5. catui-href 링크 자동 바인딩
   ↓
6. 사용자 상호작용 대기

[사용자가 링크 클릭]
   ↓
7. ViewRouter가 경로 검증 (Security)
   ↓
8. LoadingIndicator 표시
   ↓
9. HTML 파일 fetch
   ↓
10. Security가 HTML 새니타이징
   ↓
11. 컨테이너에 렌더링
   ↓
12. 스크립트 실행
   ↓
13. LoadingIndicator 숨김
   ↓
14. afterLoad 훅 실행

[사용자가 모듈 사용]
   ↓
15. ModuleLoader가 모듈 로드
   ↓
16. LoadingIndicator 표시
   ↓
17. JS/CSS 동적 import
   ↓
18. 모듈 캐싱
   ↓
19. LoadingIndicator 숨김
   ↓
20. 모듈 반환
```

---

## 🎯 구현 우선순위

### Phase 1: 기본 기능

1. Core Index (index.js) ✓
2. DOM Module (dom.js) ✓
3. Security Module (security.js) ✓
4. Utils Module (utils.js) ✓

### Phase 2: 고급 기능

1. Event Module (event.js) ✓
2. View Router (router.js) ✓
3. Loading Indicator (loading.js) ✓
4. Module Loader (loader.js) ✓

### Phase 3: 통합 및 테스트

- 코어 통합
- 단위 테스트
- 통합 테스트
- 성능 최적화

---

## 📊 성능 목표

| 항목 | 목표 |
|------|------|
| **코어 크기** | < 15KB (gzipped) |
| **초기 로딩** | < 50ms |
| **DOM 조작** | < 1ms (일반적인 경우) |
| **뷰 전환** | < 100ms |
| **모듈 로드** | < 200ms |

---

## 🧪 테스트 전략

### 단위 테스트

- 각 모듈별 독립 테스트
- 모든 public API 테스트
- 엣지 케이스 커버

### 통합 테스트

- 모듈 간 상호작용 테스트
- 실제 사용 시나리오 테스트

### 보안 테스트

- XSS 공격 시나리오 테스트
- 경로 순회 공격 테스트
- 인젝션 공격 테스트

---

## 📝 다음 단계

코어 아키텍처 설계가 완료되었습니다. 다음 문서는:

1. **API 레퍼런스 문서** - 모든 API 상세 설명
2. **개발 가이드** - 실제 구현 방법
3. **테스트 가이드** - 테스트 작성 방법

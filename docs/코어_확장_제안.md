# IMCAT UI 코어 확장 제안서

## 📋 현재 코어 모듈 (완성)

1. ✅ Security - XSS 방어, 경로 검증
2. ✅ Utils - 타입 체크, 유틸리티
3. ✅ DOM - jQuery 스타일 API
4. ✅ Event - 이벤트 버스
5. ✅ Loading - 로딩 인디케이터
6. ✅ Router - SPA 라우팅
7. ✅ API - HTTP 통신
8. ✅ Loader - 동적 모듈 로딩
9. ✅ Template - 템플릿 엔진
10. ✅ Index - 통합

**현재 크기:** ~22KB minified, ~8-10KB gzipped (예상)

---

## 🎯 추가 권장 기능 (우선순위별)

### 🔴 우선순위 HIGH - 코어에 반드시 필요

#### 1. **Storage Module** (스토리지 관리)

**이유:** 거의 모든 웹 애플리케이션에서 필요  
**크기:** ~2KB  
**기능:**

- localStorage/sessionStorage 래퍼
- JSON 자동 직렬화/역직렬화
- 만료 시간 설정
- 용량 제한 처리
- 타입 안전성

```javascript
// 사용 예시
IMCAT.storage.set('user', { id: 1, name: 'John' }, { expires: 3600 }); // 1시간 후 만료
const user = IMCAT.storage.get('user'); // { id: 1, name: 'John' }
IMCAT.storage.remove('user');
IMCAT.storage.clear();
```

**구현 복잡도:** 낮음  
**프로젝트 영향:** 높음 (대부분의 SPA에서 필요)

---

#### 2. **Form Validation** (폼 검증)

**이유:** 웹 애플리케이션의 핵심 기능  
**크기:** ~3KB  
**기능:**

- 기본 검증 규칙 (required, email, min, max 등)
- 커스텀 검증 규칙
- 실시간 검증
- 에러 메시지 표시
- DOM 통합

```javascript
// 사용 예시
const validator = IMCAT.form.validate('#login-form', {
  email: { required: true, email: true },
  password: { required: true, minLength: 8 }
});

if (validator.isValid()) {
  // 제출
}
```

**구현 복잡도:** 중간  
**프로젝트 영향:** 높음

---

#### 3. **State Management** (상태 관리)

**이유:** SPA에서 전역 상태 관리 필수  
**크기:** ~2-3KB  
**기능:**

- 리액티브 상태 (Proxy 기반)
- 상태 변경 감지
- 컴포넌트 자동 업데이트
- 간단한 스토어 패턴

```javascript
// 사용 예시
const store = IMCAT.state.create({
  user: null,
  isLoggedIn: false
});

store.watch('user', (newValue, oldValue) => {
  console.log('User changed:', newValue);
});

store.user = { id: 1, name: 'John' };
```

**구현 복잡도:** 중간  
**프로젝트 영향:** 매우 높음 (React의 useState 같은 역할)

---

### 🟡 우선순위 MEDIUM - 많이 사용되는 기능

#### 4. **HTTP Interceptors** (HTTP 인터셉터)

**이유:** 인증, 로깅, 에러 처리에 필수  
**크기:** ~1KB (API 모듈 확장)  
**기능:**

- 요청 인터셉터 (헤더 추가, 토큰 등)
- 응답 인터셉터 (에러 처리, 변환 등)
- 재시도 로직
- 타임아웃 처리

```javascript
// 사용 예시
IMCAT.api.interceptors.request.use((config) => {
  config.headers['Authorization'] = `Bearer ${token}`;
  return config;
});

IMCAT.api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.statusCode === 401) {
      // 로그아웃 처리
    }
    return Promise.reject(error);
  }
);
```

**구현 복잡도:** 낮음  
**프로젝트 영향:** 높음

---

#### 5. **Animation Utilities** (애니메이션 유틸리티)

**이유:** 부드러운 UI/UX를 위한 필수 기능  
**크기:** ~2KB  
**기능:**

- 기본 애니메이션 (fade, slide, scale)
- 커스텀 애니메이션
- requestAnimationFrame 기반
- 이징 함수
- Promise 기반 API

```javascript
// 사용 예시
await IMCAT.animate('#element').fadeIn(300);
await IMCAT.animate('#element').slideDown(400, 'ease-out');

// 커스텀 애니메이션
IMCAT.animate('#element').custom({
  from: { opacity: 0, transform: 'translateY(-20px)' },
  to: { opacity: 1, transform: 'translateY(0)' }
}, 300);
```

**구현 복잡도:** 중간  
**프로젝트 영향:** 중간 (UI/UX 개선)

---

#### 6. **URL & Query String** (URL 파싱/빌딩)

**이유:** SPA 라우팅에서 쿼리 파라미터 처리 필수  
**크기:** ~1KB (Router 모듈 확장)  
**기능:**

- 쿼리 파라미터 파싱
- 쿼리 스트링 빌딩
- URL 빌더
- 파라미터 인코딩/디코딩

```javascript
// 사용 예시
const params = IMCAT.url.parseQuery('?page=1&sort=name');
// { page: '1', sort: 'name' }

const queryString = IMCAT.url.buildQuery({ page: 2, filter: 'active' });
// '?page=2&filter=active'

IMCAT.view.navigate('users', { page: 2, sort: 'name' });
// /users?page=2&sort=name
```

**구현 복잡도:** 낮음  
**프로젝트 영향:** 높음

---

### 🟢 우선순위 LOW - 선택적 기능

#### 7. **Date & Time Utilities** (날짜/시간)

**이유:** 날짜 처리는 자주 사용되지만 외부 라이브러리 사용 가능  
**크기:** ~2KB  
**기능:**

- 포맷팅 (format, parse)
- 상대 시간 (fromNow)
- 날짜 계산 (add, subtract)
- 비교 (isBefore, isAfter)

```javascript
// 사용 예시
IMCAT.date.format(new Date(), 'YYYY-MM-DD HH:mm:ss');
IMCAT.date.fromNow(date); // '3 hours ago'
IMCAT.date.add(date, 7, 'days');
```

**구현 복잡도:** 중간  
**프로젝트 영향:** 중간 (day.js, date-fns로 대체 가능)

---

#### 8. **Observer Pattern** (옵저버)

**이유:** 컴포넌트 간 통신  
**크기:** ~1KB (Event 모듈과 유사하지만 더 구조화)  
**기능:**

- Subject/Observer 패턴
- 구독/해제
- 데이터 전달

**구현 복잡도:** 낮음  
**프로젝트 영향:** 낮음 (Event 모듈로 대체 가능)

---

#### 9. **Cookie Management** (쿠키 관리)

**이유:** 인증, 세션 관리  
**크기:** ~1KB  
**기능:**

- 쿠키 읽기/쓰기/삭제
- 만료 시간, 도메인, path 설정
- HttpOnly, Secure 플래그

```javascript
// 사용 예시
IMCAT.cookie.set('token', 'abc123', { expires: 7, secure: true });
const token = IMCAT.cookie.get('token');
IMCAT.cookie.remove('token');
```

**구현 복잡도:** 낮음  
**프로젝트 영향:** 중간

---

#### 10. **Error Boundary** (에러 경계)

**이유:** 전역 에러 처리  
**크기:** ~1KB  
**기능:**

- 전역 에러 핸들러
- 에러 로깅
- Fallback UI
- 에러 복구

```javascript
// 사용 예시
IMCAT.errorBoundary.catch((error, errorInfo) => {
  console.error('Global error:', error);
  IMCAT('#app').html('<div>Something went wrong</div>');
});
```

**구현 복잡도:** 낮음  
**프로젝트 영향:** 중간

---

## 📊 권장 구현 우선순위

### Phase 1: 필수 기능 (총 ~7KB)

1. ✅ **Storage Module** (2KB)
2. ✅ **HTTP Interceptors** (1KB)
3. ✅ **URL & Query String** (1KB)
4. ✅ **State Management** (3KB)

**이유:** 대부분의 SPA에서 필수적으로 사용

### Phase 2: 자주 사용되는 기능 (총 ~5KB)

1. ✅ **Form Validation** (3KB)
2. ✅ **Animation Utilities** (2KB)

**이유:** UI/UX 개선 및 사용자 입력 처리

### Phase 3: 선택적 기능 (총 ~5KB)

1. ⚪ **Cookie Management** (1KB)
2. ⚪ **Error Boundary** (1KB)
3. ⚪ **Date & Time** (2KB) - 또는 day.js 사용 권장

**이유:** 필요 시 추가, 외부 라이브러리로 대체 가능

---

## 🎯 최종 코어 크기 예상

| 구성 | 크기 (minified) | Gzipped |
|------|----------------|---------|
| **현재 코어** | 22KB | 8-10KB |
| **+ Phase 1** | 29KB | 11-13KB |
| **+ Phase 2** | 34KB | 13-15KB |
| **+ Phase 3** | 39KB | 15-17KB |

**목표:** Phase 1-2까지 구현하여 **~15KB (gzipped)** 유지

---

## 💡 추가 제안

### 코어 외부 (별도 모듈)로 분리 권장

- **i18n** (국제화) - 2-3KB, 필요한 프로젝트만
- **Virtual Scroll** - 3-4KB, 대용량 리스트에만
- **Drag & Drop** - 2-3KB, 특정 UI에만
- **WebSocket** - 2KB, 실시간 통신 필요 시
- **Chart/Graph** - 외부 라이브러리 사용 권장

---

## 📝 구현 가이드라인

### 각 모듈 구현 시 고려사항

1. **크기 제한:** 각 모듈 3KB 이하 (minified)
2. **의존성:** 코어 모듈만 사용, 외부 의존성 금지
3. **테스트:** 100% 커버리지 유지
4. **문서:** JSDoc + 사용 예시
5. **보안:** Security 모듈 통합
6. **성능:** 최적화 필수

---

## 🎯 다음 단계

### 즉시 구현 권장 (Phase 1)

1. **Storage Module** - 거의 모든 앱에서 필요
2. **HTTP Interceptors** - API 모듈 확장으로 쉽게 추가
3. **URL & Query String** - Router 모듈 확장

### 2순위 구현 (Phase 2)

1. **State Management** - 복잡도 중간, 영향도 높음
2. **Form Validation** - 필수 기능

---

## 📚 참고 자료

- **Storage:** [MDN Web Storage API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Storage_API)
- **State:** [Proxy API](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Proxy)
- **Animation:** [Web Animations API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Animations_API)
- **Form Validation:** [HTML5 Constraint Validation](https://developer.mozilla.org/en-US/docs/Learn/Forms/Form_validation)

---

## ✅ 결론

**가장 시급한 3가지:**

1. 🔴 **Storage Module** - 필수
2. 🔴 **HTTP Interceptors** - 필수
3. 🔴 **URL & Query String** - 필수

**권장 크기:** 코어 + Phase 1 = **~13KB gzipped**

이 정도면 경량화를 유지하면서도 실전에서 바로 사용 가능한 완전한 프레임워크가 됩니다! 🚀
